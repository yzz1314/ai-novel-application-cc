# 三份文档功能实现度评估报告

> 评估日期：2026-06-26（在 2026-06-25 基础上复核，新增记忆 DB 持久化层核对）  
> 评估对象：`系统架构设计文档.md`、`项目实现文档.md`、`架构设计文档_补充内容.md`  
> 对照代码：当前 `novel-system/`、根目录 `docker-compose.yml` 与前端/后端/Python 服务  
> 评估方式：静态代码核对 + Docker 构建验证 + 服务健康检查 + 样本导入/分析/章节分块 DB 同步/逐块分析结果 DB 同步/模型配置 DB 与 API Key 加密烟测/Skill Profile DB 同步/Skill 管理/大纲审查/章节终稿/记忆 DB 同步/图谱/检索上下文/自动返修/独立返修/版本恢复/人工审查/统一产物管理/工作流与逐块分析 checkpoint 烟测 + 前端浏览器路由烟测

> 2026-06-26 复核与开发说明：本轮先复核出代码已新增 `memory_artifacts` 记忆数据库持久化层（`MemoryArtifact` 实体、Repository、Service、`GET /memory/db`、`POST /memory/sync`，`memory_extraction` 成功后由 `TaskExecutorService` 自动同步，前端 `MemoryView` 新增同步按钮）；随后按同一模式新增了 `graph_artifacts` 知识图谱数据库持久化层（`GraphArtifact` 实体、Repository、`GraphArtifactDbService`、项目级与 book 级 `/graph/db`、`/graph/sync`，`graph_build` 成功后自动同步，前端 `GraphView` 新增同步按钮）和 `retrieval_artifacts` 检索数据库持久化层（`RetrievalArtifact` 实体、Repository、`RetrievalArtifactDbService`、`/retrieval/db`、`/retrieval/sync`、`/retrieval/list/db`，`retrieval_index` 成功后自动同步，前端 `RetrievalView` 新增同步按钮）。本次已据此上调记忆/图谱/检索系统与 Phase 1/6/7 评分，并把“记忆/图谱/检索数据库表”从缺口移除——sample/analysis/model/skill/outline/chapter/memory/graph/retrieval 九类核心产物现已全部具备 DB 业务表。Python Agent 仍为 15 个；`graph_artifacts`/`retrieval_artifacts` 两层为本轮新写代码，仅做静态核对（实体 setter↔字段、Repository 方法、Controller 注入、import 与结构核对），尚未离线编译/烟测（VM 仅 Java 11、无 Maven/Docker，项目需 Java 17）。

> 2026-06-26 增量实现说明：本次进一步补齐了知识图谱数据库持久化层（`GraphArtifact` 实体、`GraphArtifactRepository`、`GraphArtifactDbService`、`GET /api/projects/{projectId}/graph/db`、`POST /api/projects/{projectId}/graph/sync` 及对应 book 级端点和 `GET /api/projects/{projectId}/graphs/db` 列表），`graph_artifacts` 表按 `project_id+book_id` 唯一保存图谱 JSONB、节点/边列表、节点/边类型分布、平均度/密度、Top 度节点、人物/地点/组织/物品计数、最近 `graph_build` 任务和元数据，并在 `graph_build` 任务成功后由 `TaskExecutorService` 自动同步；前端 `GraphView` 新增“同步图谱数据库”按钮，`api.ts` 新增 `graphApi.getDb/syncDb`。当时表结构沿用 `ddl-auto: update` 自动创建；后续已纳入 Flyway 迁移并改为 Hibernate `validate` 校验。已据此上调知识图谱与 Phase 7 评分，并移除“图谱 DB 业务表”缺口。

> 2026-06-27 增量实现说明：本次补齐并扩展记忆连续性报告的确定性自动修复闭环。Python `MemoryQueryAgent` 在“人物出场记录未更新”问题中写入 `append_unique` 结构化 fix，并为“人物提前出场”“设定提前揭示”写入 `set_field first_mentioned` fix；Java `MemoryService` 将审计修复抽象为通用报告修复，新增 `/memory/continuity/reports/{reportId}/issues/{issueIndex}/fix`，支持应用 `append_unique` 和 `set_field`，应用前自动创建 `before_memory_continuity_fix` 记忆版本、更新报告 resolution/fix 状态，并写入 `memory_continuity_fix` 事件；前端 `MemoryView` 对连续性和审计报告统一展示“应用修复”入口。已通过 Python 连续性测试、Java `MemoryServiceTest`、Java 全量测试、前端 build 和 Playwright smoke。

> 2026-06-27 增量实现说明：本次补齐知识图谱高级分析与增量摘要的 DB/UI 闭环。`graph_artifacts` 新增 `top_nodes_by_centrality`、`top_nodes_by_betweenness`、`relationship_analysis`、`key_paths`、`incremental_summary`、`graph_analysis` JSONB 字段，并增加 Flyway `V2__add_graph_advanced_analysis.sql`；`GraphArtifactDbService` 同步时会兼容 Python 图谱产物中的 snake_case/camelCase，提取中心度、桥接节点、关系类型分析、关键路径和增量构建摘要，列表摘要也返回连通分量、中心度/关系/路径计数；前端 `GraphView` 在“高级分析”中展示增量开关、保留节点数、中心度节点数和关键路径数，并在同步 DB 后直接切换到 DB 响应数据。已通过 Java `GraphArtifactDbServiceTest`、Python `tests/test_graph_analysis.py`、前端 build 和 Playwright smoke。

> 2026-06-27 增量实现说明：本次补齐图谱版本审计闭环。`GraphArtifactService` 新增图谱版本创建、列表和详情读取，版本快照写入 `graph/versions/{bookId}/graph_{bookId}_{timestamp}.json`，包含 source path、节点/边计数、统计、分析和完整图谱；图谱重建会在创建 `graph_build` 任务前自动归档当前图谱，并把 `previous_graph_version_id/path` 注入任务参数。`GraphController` 新增项目级与 book 级 `/graph/versions` 端点；前端 `GraphView` 新增“创建版本”和“版本”抽屉，可手动创建、查看版本列表和详情。已通过 Java `GraphArtifactServiceTest` targeted 测试。

> 2026-06-27 增量实现说明：本次补齐检索索引版本治理闭环。`RetrievalArtifactService` 新增索引版本创建、列表和详情读取，版本快照写入 `indexes/versions/retrieval_index_{timestamp}.json`，包含检索配置、BM25/vector/hybrid 摘要、重建报告、质量报告、上下文包列表、文档计数和 artifact refs；缓存失效快照也兼容进入版本列表。索引重建会在创建 `retrieval_index` 任务前自动归档当前索引，并把 `previous_retrieval_version_id/path` 注入任务参数。`RetrievalController` 新增 `/retrieval/versions` 端点；前端 `RetrievalView` 新增“创建版本”和“版本”抽屉，可查看最新版本和详情。已通过 Java `RetrievalArtifactServiceTest` targeted 测试和前端 `npm run build`。

> 2026-06-27 增量实现说明：本次补齐离线检索效果基准闭环。Python `HybridRetrievalEngine` 新增 `evaluate_benchmark`，支持从请求参数 `benchmark_queries` 或 `indexes/retrieval_benchmark.json` 读取固定查询集，计算 hit rate、MRR、recall@k、平均质量分和逐用例命中明细，并写入 `indexes/retrieval_benchmark_report.json`；`retrieval_index` 报告和 hybrid summary 会汇总 benchmark 摘要。Java `RetrievalArtifactService` 新增 `/retrieval/benchmark` 读取和 `/retrieval/benchmark/evaluate` 触发入口；前端 `RetrievalView` 新增“运行基准”和“检索效果基准”卡片。已通过 Python `tests/test_retrieval_quality_budget.py` 和 Java `RetrievalArtifactServiceTest` targeted 测试。

> 2026-06-27 增量实现说明：本次补齐检索 benchmark 报告的数据库聚合持久化。`RetrievalArtifact` 新增 `benchmark_report_path` / `benchmark_report` 字段，Flyway 新增 `V4__add_retrieval_benchmark_report.sql`；`RetrievalArtifactDbService` 同步时会读取 `indexes/retrieval_benchmark_report.json`，在 `/retrieval/db` 和 `/retrieval/list/db` 响应中暴露 benchmark 状态、用例数、通过数、hit rate 和 MRR，并写入 `retrievalMetadata.latestBenchmarkReport` 与摘要指标。`RetrievalArtifactDbServiceTest` 已覆盖 benchmark 报告落库、元数据汇总和列表摘要。

> 2026-06-27 增量实现说明：本次补齐 Skill 冲突检测的报告化与 DB 元数据闭环。Java 新增 `POST /api/projects/{projectId}/skills/conflicts/report`，可显式生成 `novel/reviews/skills/conflicts_skill_conflict_report_{timestamp}.json`，报告包含冲突数量、severity 汇总、冲突明细和处理建议；`SkillService` 会把最新冲突报告路径、检查人、检查时间、severity 汇总写入 `skill_profiles.skill_metadata.latestConflictReport`，`GET /skills/conflicts` 也会回显最近报告路径。前端 ProjectDetail 的 Skills 标签页新增“生成冲突报告”入口，并在冲突提示中展示报告路径和建议。已通过 `SkillServiceSemanticQualityTest`、Java 全量测试和前端 `npm run build`。

> 2026-06-28 增量实现说明：本次补齐 Skill 历史版本与当前版本的差异对比闭环。Java 新增 `GET /api/projects/{projectId}/skills/{skillName}/versions/{versionId}/diff`，可直接比较指定 Skill 版本快照与当前 `skills/local/{skillName}.md`，返回左右版本摘要、line limit、截断标记、added/removed/changed 行数和行级 diff；前端 ProjectDetail 的 Skill 历史版本弹窗新增“对比当前”操作和差异弹窗，按 added/removed/unchanged 展示行级变化。已通过 `SkillServiceSemanticQualityTest` 定向测试和前端 `npm run build`。

> 2026-06-28 增量实现说明：本次补齐 Skill 重新生成入口和生成状态反馈。前端 `skillsApi.generate` 现在会同时把生成配置写入 `config` 与 `parameters`，与 Python `SkillGeneratorAgent` 读取 `request.parameters.skill_types` 的实现对齐；ProjectDetail 的 Skills 标签页即使暂无 Skill 也会展示工具栏，新增“重新生成Skills”按钮，自动携带 `regenerate=true`、`skill_types=["writing","outline","review"]`、已分析样本 ID 和当前 Skill 数量创建 `skill_generation` 任务，并展示最近一次 Skill 生成任务的状态、任务 ID、创建时间、生成数量或错误信息。已通过前端 `npm run build`。

> 2026-06-28 增量实现说明：本次补齐 Skill 生成结果预览入口。ProjectDetail 的“最近一次Skill生成任务”提示新增“查看生成结果”，打开 Drawer 后可查看任务状态、Agent、创建时间、生成数量、样本数量、`enabled_yaml` 路径、`generated_skills` 明细表、`outputRefs` 和原始 result JSON。已通过前端 `npm run build`。

> 2026-06-28 增量实现说明：本次补齐 Skill 报告预览/下载入口。ProjectDetail 复用统一产物 API，新增 Skill 报告 Drawer，可从 Skill 列表、质量详情、冲突提示和批准/驳回动作直接预览或下载 `skill_quality_check`、`skill_approval`、`skill_rejection` 与 `skill_conflict_report` JSON 报告，不再只展示报告路径。已通过前端 `npm run build`。

> 2026-06-28 增量实现说明：本次补齐大纲 JSON 历史版本治理。Java 新增 `GET /api/projects/{projectId}/books/{bookId}/outline/versions`、`GET /outline/versions/{versionId}` 和 `POST /outline/versions/{versionId}/restore`，可读取 `novel/outline/versions/{bookId}` 下的编辑前快照、预览完整大纲 JSON，并在恢复前再次归档当前大纲；恢复会遵守大纲锁定保护，除非显式传入 `overrideOutlineLock`，恢复后将治理状态置为 `pending_review` 并同步 `outline_artifacts`。前端 OutlineEditor 新增“大纲版本”入口和历史版本抽屉，可预览并恢复大纲快照。已补 `BookArtifactServiceTest` 定向覆盖。

> 2026-06-28 增量实现说明：本次补齐记忆审计 `sort_by_chapter` 自动修复的 Java 定向验证。`MemoryQueryAgent` 已能为“时间线排序倒挂”生成 `fix.action=sort_by_chapter`，Java `MemoryService` 已支持按章节重排 `timeline.json`；本次新增 `MemoryServiceTest` 覆盖应用时间线排序修复、恢复前记忆版本、报告 `fix_applied` 状态和 `memory_audit_fix` 事件归档。

> 2026-06-28 增量实现说明：本次补齐伏笔节点的图谱关系抽取。Python `GraphBuilderAgent` 现在会为 `suspenses.json` 中的 `involved_characters`、`involved_settings` 生成 `involves_character` / `involves_setting` 边，并支持 `resolved_by_plot` / `resolved_by` / `resolution_plot` 生成伏笔到剧情节点的 `resolved_by` 边，避免伏笔节点只入图但缺少关系。已扩展 `tests/test_graph_analysis.py` 覆盖伏笔-人物、伏笔-剧情关系。

> 2026-06-28 增量实现说明：本次补齐大纲结构入图。`GraphBuilderAgent` 现在会在 `use_outline_data=true` 时读取 `novel/outline/{bookId}_outline.json`（兼容旧 `outlines` 路径），把大纲顶层人物、世界设定、长线悬念和章节大纲纳入图谱；章节大纲会生成 `chapter_outline` 节点，并基于章节目标、人物发展、信息揭示、场景、伏笔、边界字段等文本确定性匹配已知人物/设定/伏笔/剧情，生成 `involves_character`、`involves_setting`、`sets_up_foreshadowing` 和 `advances_plot` 边。已扩展 `tests/test_graph_analysis.py` 覆盖章节大纲到人物、地点、伏笔和剧情节点的关系。

> 2026-06-28 增量实现说明：本次补齐章节正文确定性入图。`GraphBuilderAgent` 现在会读取 `novel/chapters/final/{bookId}/volume_N/chapter_X.json`、`novel/chapters/drafts/{bookId}/volume_N/chapter_X.json` 和兼容 book 路径中的章节 JSON，优先终稿、再草稿，把含 `content` 的章节生成 `chapter_content` 节点；正文节点会连接对应 `chapter_outline` 节点，并基于正文内容匹配已知人物、设定、伏笔和剧情节点，生成 `mentions_character`、`mentions_setting`、`mentions_foreshadowing` 和 `mentions_plot` 边。已扩展 `tests/test_graph_analysis.py` 覆盖章节正文到大纲、人物、地点、伏笔和剧情节点的关系。

> 2026-06-28 增量实现说明：本次补齐样本技巧关系入图。`GraphBuilderAgent` 现在会读取 `analysis/cross_book/technique_summary.json`（兼容项目根目录 `technique_summary.json`），把跨书归纳的 `scene_techniques`、`prose_techniques`、`outline_techniques` 聚合为 `technique` 节点，保留分类、分类计数、总次数和来源路径；章节大纲与章节正文若匹配到技巧名，会生成 `uses_technique` 边。已扩展 `tests/test_graph_analysis.py` 覆盖大纲章节使用大纲技巧、正文使用场景技巧的关系。

> 2026-06-28 增量实现说明：本次补齐统一产物治理能力的定向验证与评估报告修正。既有 `ArtifactController` / `ArtifactService` / `ArtifactView` 已支持产物归档、恢复、归档永久删除、批量 ZIP 导出、文本差异对比、审计日志查询和敏感样本授权保护；本次新增 `ArtifactServiceTest` 覆盖批量导出默认跳过敏感样本、导出 manifest 记录跳过原因、bulk_export 审计统计，以及审计事件按最近优先和 limit 返回。已通过 Docker Maven `ArtifactServiceTest` 定向测试。

> 2026-06-28 增量实现说明：本次补齐统一产物治理的角色门槛与保留策略闭环。`ArtifactService` 新增操作级 role 校验，敏感完整预览/下载/diff/批量导出要求 owner/admin/artifact_manager/auditor，归档/恢复/归档永久删除/保留策略要求 owner/admin/artifact_manager，审计查询要求 auditor 级角色；拒绝操作会写入 `*_denied` 审计事件。新增 `POST /api/projects/{projectId}/artifacts/retention/apply`，支持 30 天默认归档保留策略、dry-run 预览和实际清理，并在 `ArtifactView` 增加“保留策略”入口。已通过 Docker Maven `ArtifactServiceTest` 10 用例和前端 `npm run build`。

> 2026-06-28 增量复核说明：本次复核模型配置运行时 secrets 隔离现状。`ModelProfileService` 已将 DB 中加密 API Key 导出为 `apiKeyRef`，`config/model_profiles.json` 仅保留 secret 引用和 `secretsFile=config/model_profile_secrets.json`，实际 key 单独写入 `config/model_profile_secrets.json`；Python `LLMClient` 已支持从该 secrets 文件解析 `apiKeyRef`，运行时配置文件不再落明文 API Key。已通过 Docker Maven `ModelProfileServiceTest` 与 Python `tests/test_llm_gateway.py -k runtime_secret_reference` 验证。后续仍需生产级 secrets provider、轮换、权限审计和正式跨服务鉴权。

> 2026-06-28 增量实现说明：本次补强并复核 Python LLM Gateway 基础治理能力。`LLMClient` 已支持按 profile 的 fallback model 顺序重试、响应缓存、token/成本估算、用量 JSONL 台账、请求级 usage summary、`min_interval_ms` / RPM 节流，以及 embedding/rerank 的本地降级；本次把限流测试从“间隔计算”扩展为实际 `_apply_rate_limit` 行为验证。已通过 `python -m pytest tests/test_llm_gateway.py`。后续仍需真实供应商 API Key 集成测试、生产级配额/成本看板和集中 secrets/权限治理。

> 2026-06-28 增量实现说明：本次收紧数据库迁移治理默认配置。Java 默认 `spring.jpa.hibernate.ddl-auto` 已从 `update` 改为 `validate`，根目录与 `novel-system/` 两个 Docker Compose 入口均显式传入 `SPRING_JPA_HIBERNATE_DDL_AUTO=validate`，schema 变更由 Flyway `db/migration` 迁移文件负责；新增 `SchemaMigrationConfigurationTest` 守护默认/prod/ci 配置，避免回退到 Hibernate 自动建表。已通过 Docker Maven `-Dtest=SchemaMigrationConfigurationTest test`。后续仍需迁移回滚策略、线上变更评审和跨环境迁移演练。

> 2026-06-28 增量复核说明：本次复核 Dashboard 与监控入口状态。`DashboardController` 已暴露 `/api/dashboard`，`DashboardService` 聚合项目/样本/章节/任务统计、任务状态分布、服务健康、最近项目、最近任务、工作流进度、阻塞项目、下一步动作和最近 50 个任务性能窗口（耗时、慢任务、高重试、LLM 调用、token 与按任务类型聚合）；前端 `Dashboard.tsx` 已通过 `dashboardApi.getOverview()` 展示这些真实数据块。已通过 Docker Maven `DashboardServiceTest` 和前端 `npm run build` 验证。后续仍需生产级连接治理、正式权限审计、CI 与更多固定 E2E。

> 2026-06-28 增量复核说明：本次复核 ProjectList 与项目创建链路。前端 `ProjectList.tsx` 创建请求已传递 `genre` 和 `sampleGroupType`，状态标签已覆盖 `CREATED`、`INGESTING`、`ANALYZED`、`OUTLINING`、`WRITING`、`ARCHIVED` 等当前后端枚举；后端 `CreateProjectRequest`、`ProjectController`、`ProjectService` 与 `ProjectResponse` 已接收、归一化、保存并回显 `genre`。已通过 Docker Maven `ProjectServiceTest` 验证。后续 ProjectList 主要剩余筛选/排序、批量操作和更细项目状态引导等体验增强。

> 2026-06-28 增量实现说明：本次补齐 ProjectList 的列表筛选、排序、批量归档和状态引导。前端 `ProjectList.tsx` 新增项目名称/题材/描述/ID 搜索、状态筛选、样本分组筛选、清空筛选、刷新、表格列排序、行选择和“批量归档”；单项操作文案从“删除”调整为更符合后端语义的“归档”，归档项目不可重复选择；状态列旁新增“下一步”引导，并接通 Dashboard 的 `/projects?action=create` 创建入口。已通过前端 `npm run build` 验证。后续 ProjectList 主要剩余更细项目状态与真实工作流状态联动。

> 2026-06-28 增量实现说明：本次补齐任务日志实时推送基础闭环。Java `TaskController` 新增 `GET /api/tasks/{taskId}/logs/stream` SSE 端点，每秒推送任务日志 snapshot，终态任务发送 `complete` 事件后结束；前端 `TaskCenter` 在任务详情抽屉打开时通过 `EventSource` 订阅该 stream，实时刷新事件表、文件日志 Tail、进度和任务列表对应行，并展示“连接中/实时日志/日志已结束/实时连接断开”状态。已通过 Docker Maven `TaskControllerTest` 和前端 `npm run build` 验证。后续仍需长期趋势存储、告警闭环、生产级连接治理和权限鉴权。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 基础告警中心。`DashboardService` 在 `/api/dashboard` 响应中新增 `alerts` 聚合，覆盖 Python AI 服务不可用、失败任务、待人工确认、慢任务、高重试任务和阻塞项目等基础告警，并附带 severity、target、details 与创建时间；前端 `Dashboard.tsx` 新增“告警中心”卡片，可展示告警级别、说明并跳转到对应任务/项目/服务处理入口，无告警时显示成功状态。已通过 Docker Maven `DashboardServiceTest`、前端 `npm run build` 和 `git diff --check` 验证。后续仍需长期趋势存储、生产级告警通知/确认/升级策略、连接治理和权限鉴权。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 告警确认/静默基础闭环。Java 新增 `dashboard_alert_states` 表、`DashboardAlertState` 实体与 Flyway V5 迁移，`POST /api/dashboard/alerts/{alertId}/state` 支持 `acknowledge`、`snooze` 和 `reopen`，并用 `conditionKey` 保证确认/静默只压制同一告警条件，条件变化后重新变为活跃告警；`/api/dashboard` 会返回 `alerts`、`snoozedAlerts`、`acknowledgedAlerts` 与 `alertSummary`。前端 Dashboard 告警中心新增“确认”“静默1小时”和状态/计数展示。已通过 Docker Maven `DashboardServiceTest,SchemaMigrationConfigurationTest`、前端 `npm run build` 和 `git diff --check` 验证。后续仍需长期趋势存储、外部通知渠道、告警升级规则、生产级连接治理和权限鉴权。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 基础长期趋势快照。Java 新增 `dashboard_metric_snapshots` 表、`DashboardMetricSnapshot` 实体与 Flyway V6 迁移，`/api/dashboard` 每次生成概览时记录健康状态、活跃/失败/待审批任务、慢任务、高重试、告警计数、token 与耗时等指标快照；新增 `GET /api/dashboard/trends?limit=...` 返回最近快照、健康状态、失败任务/活跃告警/token delta。前端 Dashboard 新增“长期趋势”卡片，展示最近快照与失败/告警/token 变化。已通过 Docker Maven `DashboardServiceTest,SchemaMigrationConfigurationTest`、前端 `npm run build` 和 `git diff --check` 验证。后续仍需正式趋势图表、采样/保留策略、外部通知渠道、告警升级规则、生产级连接治理和权限鉴权。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 趋势图表与基础采样/保留策略。`DashboardService` 现在会按 `DASHBOARD_TREND_SAMPLE_INTERVAL_MINUTES` 控制快照采样间隔，避免每次刷新都写入密集快照；按 `DASHBOARD_TREND_RETENTION_DAYS` 清理过期 `dashboard_metric_snapshots`，并在 `latestTrend.retention` 和 `/api/dashboard/trends` summary 中返回采样/保留策略。前端 Dashboard 长期趋势卡片新增 SVG 折线图，展示失败任务、活跃告警和 token 三条趋势，并显示采样间隔与保留天数。已扩展 `DashboardServiceTest` 覆盖采样跳过和保留清理。后续仍需生产级连接治理、正式权限审计、CI 与更多固定 E2E。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 告警通知 outbox 与基础升级规则。Java 新增 `dashboard_alert_notifications` 表、`DashboardAlertNotification` 实体与 Flyway V7 迁移，`/api/dashboard` 会把活跃 warning/critical 告警转为去重通知事件，critical 自动标记 `ESCALATE`、warning 标记 `NOTIFY`，并根据 `DASHBOARD_ALERT_WEBHOOK_URL`、`DASHBOARD_ALERT_EMAIL_TO` 等配置暴露 webhook/email 渠道状态；新增 `GET /api/dashboard/alerts/notifications?limit=...` 查询最近通知事件和汇总。前端 Dashboard 新增“告警通知与升级”卡片，展示 Dashboard/Webhook/Email 渠道配置、待配置数量、升级数量和最近通知事件。已通过 Docker Maven `DashboardServiceTest,SchemaMigrationConfigurationTest`、前端 `npm run build` 和 `git diff --check` 验证。后续仍需真实 webhook/email 发送器、重试/投递回执、通知订阅人和生产级连接治理。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 告警外部投递、回执和重试基础闭环。Java 新增 `DashboardAlertDeliveryService`，支持通过 `DASHBOARD_ALERT_WEBHOOK_URL` 发送 webhook、通过 Spring Mail 和 `DASHBOARD_ALERT_EMAIL_TO` / `DASHBOARD_ALERT_EMAIL_FROM` 发送邮件；`dashboard_alert_notifications` 新增 delivery status、attempts、receipt、last delivery、next retry 字段并由 Flyway V8 管理。Dashboard 生成通知事件后会按配置尝试投递，成功记为 `DELIVERED`，失败记为 `RETRY_PENDING` 并按 `DASHBOARD_ALERT_DELIVERY_RETRY_DELAY_MINUTES` 安排重试，达到 `DASHBOARD_ALERT_DELIVERY_MAX_ATTEMPTS` 后记为 `FAILED`；未配置外部渠道时保持 `PENDING_CHANNEL`。前端 Dashboard 通知卡片新增投递状态、重试数、失败数、投递次数和下次重试时间。已通过 Docker Maven `DashboardServiceTest,SchemaMigrationConfigurationTest`、前端 `npm run build` 和 `git diff --check` 验证。后续仍需生产级连接治理。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 告警通知订阅人、分组路由和模板治理基础闭环。Java 新增 `dashboard_alert_notification_policies` 表、`DashboardAlertNotificationPolicy` 实体、Repository 与 Flyway V9 迁移，提供 `GET/POST /api/dashboard/alerts/notification-policy` 读取和更新默认通知策略；策略可保存启停、默认组、订阅人、critical/warning 路由、渠道开关和标题/正文模板。`DashboardService` 生成告警通知时会把命中的接收组、订阅人邮箱和渲染模板写入通知 payload，`DashboardAlertDeliveryService` 邮件投递会合并环境变量收件人与策略收件人，并优先使用策略模板。前端 Dashboard 通知卡片新增策略摘要、订阅人/分组展示和“通知策略”编辑弹窗。已通过 Docker Maven `DashboardServiceTest,DashboardAlertDeliveryServiceTest,SchemaMigrationConfigurationTest`、前端 `npm run build` 和 `git diff --check` 验证。后续仍需生产级连接治理。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 告警投递历史筛选。`GET /api/dashboard/alerts/notifications` 新增 `deliveryStatus`、`escalationLevel`、`severity`、`status`、`alertId`、`since`、`until` 查询参数，后端通过 JPA Specification 按投递状态、升级级别、告警等级、通知状态、告警 ID 和最近出现时间过滤，并返回 `filters`、`matchedTotal` 与筛选后的汇总。前端 Dashboard 告警通知卡片新增投递状态、升级级别、告警等级、告警 ID、起止日期筛选、清空和命中数量展示。已扩展 `DashboardServiceTest` 覆盖筛选参数与汇总。后续仍需生产级连接治理和更完整的权限审计。

> 2026-06-28 增量实现说明：本次补齐 Dashboard 正式运维看板基础闭环。Java 新增 `GET /api/dashboard/operations`，只读聚合 readiness、incidents、capacity、delivery、dataFreshness 和 runbook，不触发趋势快照或告警通知写入；看板可展示服务可用性、活跃/严重告警、阻塞项目、任务积压、慢任务/高重试、通知投递异常、趋势快照新鲜度和下一步处理动作。前端 Dashboard 新增“运维看板”卡片，展示就绪状态、趋势新鲜度、活跃告警、任务积压、投递异常、趋势年龄和 runbook 操作入口。已扩展 `DashboardServiceTest` 覆盖 readiness、incident、delivery、freshness 和 runbook 汇总。后续仍需生产级连接治理、正式权限审计、CI 与更多固定 E2E。

> 2026-06-28 增量实现说明：本次补强固定 MVP smoke 脚本的验收输出。`novel-system/scripts/smoke_mvp_pipeline.py` 已覆盖离线 mock 链路：样本导入 -> 全文分析 -> 覆盖率校验 -> 单书报告 -> 跨书归纳 -> Skill 生成，并检查 `analysis/per_chunk`、`analysis/per_book`、`analysis/cross_book`、`skills/enabled.yaml` 等关键产物；本次新增每个样本的 `CHUNKED`/`ANALYZED` 语义状态断言和 `pipeline_checks` 输出，测试 `tests/test_smoke_mvp_pipeline.py` 已同步覆盖。已通过 `python -m pytest tests/test_smoke_mvp_pipeline.py`。

> 2026-06-28 增量实现说明：本次补齐大纲批准前的确定性质量门槛。Java `BookArtifactService.updateOutlineGovernance(..., "approve", ...)` 现在会读取当前大纲并校验每个章纲必须具备 `core_goal`、`must_write`、`allowed_progress`、`must_not_write`、`reserved_for_future`、`stop_point`、`ending_hook` 七类边界字段；缺失时默认拒绝批准，显式 `overrideOutlineApproval=true` 才允许人工强制放行，并把 `approvalCheck`、缺失项和 `approvalOverride` 写入治理元数据。已通过 Docker Maven `BookArtifactServiceTest` 10 个用例验证。后续仍需逐章审批 UI、结构化编辑和真实 LLM 质量评估。

> 2026-06-28 增量实现说明：本次补强任务日志文件尾读能力。`GET /api/tasks/{taskId}/logs` 新增 `tailLines` 参数，在结构化 `events` 之外返回 `eventLogTail`（path、requestedLines、exists、totalLines、startLine、lineCount、truncated、lines）；TaskCenter 日志抽屉新增“文件日志 Tail”卡片展示原始 JSONL 尾部，便于排查长任务事件和失败重试。已通过 Docker Maven `TaskExecutorServiceCancellationTest` 和前端 `npm run build` 验证。后续仍需长期趋势存储和告警闭环。

> 2026-06-28 增量实现说明：本次补齐失败/缺失 chunk 的聚合诊断与前端详情入口。Java 新增 `GET /api/projects/{projectId}/analysis/samples/{sampleId}/issues`，把覆盖率报告、repair queue、chunk 元信息和失败分析文件路径合并为 `issues` 列表（类型、优先级、错误、chunk/analysis 路径、章节/位置、预览和修复队列项）；SampleManagement 覆盖率页签改为优先展示该聚合结果，并支持从问题块直接打开分块详情或单块定向修复。已通过 Docker Maven `AnalysisArtifactServiceTest` 和前端 `npm run build` 验证。后续仍需真实 LLM 下的分析质量评估与失败原因分类。

> 2026-06-28 增量实现说明：本次补齐样本分析页的报告导出入口。SampleManagement 样本详情抽屉新增“导出分析包”，可一次导出样本信息、产物摘要、覆盖率、问题块、单书报告和分块索引；覆盖率页签新增“导出覆盖率”和“导出问题块”，Manifest 页签新增“导出 Manifest”，分块页签新增“导出分块索引”。已通过前端 `npm run build` 验证。后续仍需生产级任务监控治理。

> 2026-06-28 增量实现说明：本次补齐样本导入任务的细粒度百分比。Python `SampleImportAgent` 在读取原文、规范化、保存规范化文本、章节检测、分块、覆盖率校验、保存 chunks、保存 manifest 和完成阶段写入 `sample_import` checkpoint，并在成功响应中返回 `progress_percent=100` 与最终 `checkpoint_ref`；Java `TaskExecutorService.getTaskProgress` 可在任务表尚未写入 `checkpointRef` 时自动读取当前任务的 latest checkpoint，支持 `total_steps/processed_steps`、stage/stageLabel/unit 元数据和阶段化 label；SampleManagement 运行中任务进度条优先显示后端 `progress.percent` 与阶段文案，不再用已分析块数粗略推断导入进度。已通过 Python `tests/test_sample_import_progress.py`、Docker Maven `TaskExecutorServiceCancellationTest` 和前端 `npm run build` 验证。后续仍需长期趋势存储和告警闭环。

> 2026-06-28 增量实现说明：本次补强 GitHub Actions 正式 CI 基础。`.github/workflows/ci.yml` 已覆盖 push/PR/manual 触发下的 Python 测试 + 固定离线 MVP smoke、Java Maven 测试 + Flyway 迁移校验、前端 build + Playwright browser smoke；本次新增只读权限、固定 mock/workspace 环境、Python JUnit 报告、Maven surefire 报告、Playwright/build artifacts 和 smoke workspace 关键产物上传，便于失败后复盘。`.github/workflows/python-smoke.yml` 也补齐并发控制、JUnit 报告和 smoke artifacts。后续仍需覆盖率门槛、性能回归样例、更多跨服务 E2E 和发布门禁。

> 2026-06-28 增量实现说明：本次补齐正式权限系统的基础骨架。Java 新增请求头身份上下文（`X-User-Id`、`X-Actor`、`X-Role(s)`、`X-Org-Id`、`X-Project-Ids`）、`AccessControlInterceptor`、项目访问校验、mutation 角色门槛、403 异常处理和 `security.access.enforce/require-authentication` 配置；prod 默认强制认证与项目访问，dev 默认兼容本地。`PythonClientService` 会向 Python Agent 转发当前身份/组织/角色/project header，ArtifactController 也改为优先使用请求头身份，避免 body/query 中伪造 `actor/role` 提权；前端 axios 统一注入可配置身份 header。已通过 `AccessControlServiceTest`、`ArtifactControllerAccessTest`、`ArtifactServiceTest` 和 `SchemaMigrationConfigurationTest` 定向验证。后续仍需 JWT/OAuth 登录、用户/组织/成员表、细粒度 RBAC 策略和权限审计 UI。

## 1. 总体结论

当前代码已经不只是静态脚手架：**项目创建 -> 样本上传 -> Python 样本导入 -> 分块/manifest 落盘 -> Java 任务和样本状态回写 -> mock 全文分析 -> 单书报告 -> 双样本跨书归纳 -> 项目级 Skill 生成与读取** 这条 MVP 链路已经跑通。

但三份文档定义的是一个完整的“样本拆解 + 技法沉淀 + Project Soul + 大纲 + 正文创作 + 记忆 + 图谱 + 检索 + 编排 + 前端管理”系统。以这个完整目标衡量，当前整体仍处于 **MVP 骨架和局部闭环阶段**，不是“功能基本完成”。

粗略估算：

| 功能域 | 当前实现度 | 判断 |
| --- | ---: | --- |
| 基础架构与 Docker 部署 | 75% | Java、Python、React、PostgreSQL/pgvector、Redis、共享 workspace 均已接入，Docker 构建与健康检查通过 |
| Java 项目/样本/任务基础 API | 98% | 项目、样本、任务的基础 CRUD/执行已具备；样本章节/分块 `sample_chapters`、`sample_chunks` DB 同步和查询接口已补齐；逐块分析结果 `analysis_results` DB 同步、JSONB 原文保存、摘要字段查询和成功分析任务后自动同步已补齐；模型配置已迁入 `model_profiles` 表并验证 API Key 加密存储、接口脱敏、默认配置和任务默认 `model_profile_id` 注入；Skill 已新增 `skill_profiles` DB 聚合快照，保存 enabled skills、内容快照、启用配置、task overrides、质量/审批元数据，并支持手工/自动同步；大纲已新增 `outline_artifacts` DB 业务表，保存大纲 JSONB、Project Soul、最新审查报告、边界统计和摘要字段，并支持手工/自动同步；章节已新增 `chapter_artifacts` DB 业务表，按草稿/终稿阶段保存章节正文、完整 JSONB、边界检查、返修/人工审查/手工编辑历史、最新审查报告和摘要字段，并支持手工/自动同步；记忆已新增 `memory_artifacts` DB 业务表，保存 6 类 Markdown 记忆、权威 JSON、最新快照及快照列表、记忆任务、人物/世界观/剧情/伏笔/时间线计数等摘要字段，并在 `memory_extraction` 成功后自动同步、支持 `/memory/db` 查询和 `/memory/sync` 手工同步；图谱已新增 `graph_artifacts` DB 业务表，按 `project_id+book_id` 保存图谱 JSONB、节点/边列表、类型分布、度统计和图谱任务，并在 `graph_build` 成功后自动同步、支持 `/graph/db` 查询和 `/graph/sync` 手工同步；检索已新增 `retrieval_artifacts` DB 业务表，保存检索配置、BM25/vector/hybrid 索引摘要、重建报告、上下文包列表、检索任务和文档计数，并在 `retrieval_index` 成功后自动同步、支持 `/retrieval/db` 查询和 `/retrieval/sync` 手工同步；Skill 读取/编辑/启停/优先级/作用域/冲突检测/版本列表/版本恢复、规则型质量校验、批准/驳回审批、大纲、Project Soul、章节草稿/终稿、章节返修任务、版本快照/恢复、人工审查确认、章节手工编辑保存、大纲/Project Soul 编辑保存、审查归档、样本分析产物、记忆产物、图谱产物、检索上下文包只读/查询/导出、统一产物列表/预览/下载/归档/恢复/批量导出/差异对比/审计/角色校验/保留策略 API 已补齐；请求头身份上下文、项目访问校验、mutation 角色门槛和 Java->Python 身份转发已补基础骨架；仍缺完整工作流编排、JWT/OAuth 登录、用户/组织成员表和细粒度 RBAC |
| Python Agent 框架 | 80% | FastAPI + 15 个 Agent 注册可用，协议已与 Java 初步对齐；样本、分析、覆盖率校验、Skill、大纲生成、大纲审查、章节、独立 RevisionAgent、记忆、图谱、ContextBuilder、BoundaryChecker、本地混合检索、独立 `retrieval_index` 重建 Agent、`workflow` 编排 Agent 和 `full_text_analysis` 逐 chunk checkpoint/resume 已有可运行实现；workflow 已支持人工确认暂停/恢复、并行节点和条件节点；检索侧已写入质量评估、引用预算和缓存状态；缺 LangGraph 持久状态、正式向量库、真实 embedding 与外部 reranker |
| 样本导入/规范化/章节识别/分块 | 91% | 已烟测通过，能回写 `CHUNKED`；BOM 已处理；manifest 内有文本覆盖率，新增 `coverage_check` 可生成权威覆盖率报告；`sample_chapters`、`sample_chunks` 已能从 manifest/chunk 文件同步入库，且失败/错误分析文件不会再误标为已处理 |
| 全文分析/单书/跨书分析 | 70% | mock LLM 下全文分析、单书报告、双样本跨书归纳已跑通；全文分析完成后会写 `analysis/coverage/{sampleId}_coverage.json`，覆盖文本分块和逐块分析覆盖率；`full_text_analysis` 已支持逐 chunk checkpoint、复用已落盘分析结果、resume 后跳过已完成 chunk；`coverage_check` 已能生成失败/缺失 chunk 修复队列并支持 `auto_repair` 调用 `analysis_repair`；Java 侧已新增 `analysis_results` 表和 `/analysis/db`、`/analysis/sync`、`/chunks/{chunkId}/analysis/db`、`/issues` 聚合诊断接口，能同步并查询逐块分析摘要、失败/缺失问题块详情与完整 JSONB，且覆盖率任务完成后会回写 `sample_chunks.processed` 与 `analysis_results.status`；任务创建已支持 `retry_policy`/`max_retries` 配置并记录指数退避诊断；真实 LLM 和分析质量校验仍需加强 |
| Skill 系统 | 80% | 项目级 `skills/local`、`enabled.yaml`、Skill 生成/重新生成入口、生成状态与结果预览、下游加载、Java 读取/编辑/启停/优先级/作用域配置、版本列表/恢复、版本差异对比、快照归档、编辑/恢复报告、基础冲突检测、规则型质量校验、语义质量详情、批准/驳回审批已跑通；新增 `skill_profiles` 表和 `/skills/profile`、`/skills/profile/sync`，可将 Skill 内容、启用配置、task overrides、质量/审批元数据同步到 DB；Skill 冲突检测已支持显式生成审计报告，报告路径和 severity 汇总会写入 `skill_profiles.skill_metadata.latestConflictReport`；前端 ProjectDetail 已支持查看、编辑、启停、质量校验、批准/驳回、质量/审批/冲突报告预览下载、历史版本恢复、历史版本对比当前、配置版本恢复、冲突提示、冲突报告生成、重新生成任务创建和最近生成结果预览；仍缺真实模型语义评审和更深层冲突自动合并 |
| 大纲生成 | 70% | 有 OutlineGenerator、OutlineReviewAgent 和 schema；大纲保存已优先写 `novel/outline` 并兼容旧路径；`project_soul.md` 已生成；章节 schema 已补边界字段；OutlineGenerator 保存前会兜底补齐章节边界字段，OutlineReviewAgent 支持只审查、自动补齐和覆盖重建边界；Java/前端已支持大纲 JSON、Project Soul 手工编辑、锁定/解锁/批准、快照、版本列表/预览/恢复、报告归档、审查报告查看和边界补齐入口；大纲批准前已增加边界字段完整性门槛，缺失字段默认拒绝批准，显式 override 会写入治理审计；新增 `outline_artifacts` 表与 `/outline/sync`、`/outline/db`、`/books/outlines/db`，可把大纲 JSONB、Project Soul、最新审查报告、边界统计和摘要字段同步入库，并在大纲生成、审查、手工保存和大纲版本恢复后自动同步；仍缺更细粒度结构化编辑、逐章审批确认和真实 LLM 质量评估 |
| 正文创作 | 85% | 有 ChapterWriter 雏形和审查 prompt；Skill 读取、Project Soul 注入、检索上下文包注入、BoundaryChecker、后续章纲保护清单、独立 RevisionAgent、返修版本快照、历史版本恢复、章节级人工审查确认、章节手工编辑保存、`novel/reviews` 归档、草稿保存、终稿发布、终稿覆盖前快照、终稿版本递增、恢复为终稿、草稿/终稿/历史版本差异对比、批量终稿发布和 `chapter_finalize` 审计已对齐；新增 `chapter_artifacts` 表与 `/chapters/sync`、`/chapters/db`、单章 `/sync`、单章 `/db`，可将草稿/终稿正文、完整章节 JSONB、边界检查、返修历史、人工审查历史、手工编辑历史和最新审查报告同步入库，并在章节写作、返修、终稿、版本恢复、人工审查、手工编辑后自动同步；`chapter_pipeline` 工作流已有人工确认节点和前端审批/恢复入口；仍缺真实 LLM 下的返修质量验证 |
| 记忆系统 | 55% | MemoryExtractor/Query、项目级 `memory/*.md` 同步、JSON 权威文件、章节快照、Java 记忆 API、前端 MemoryView、终稿后自动摄取最小闭环已打通；新增 `memory_artifacts` DB 业务表，按项目唯一保存 6 类记忆 Markdown 快照、记忆 JSON、人物/世界观/剧情/伏笔/时间线条目计数、快照列表、最新快照、最近记忆任务和元数据，支持 `GET /memory/db` 查询、`POST /memory/sync` 手工同步，并在 `memory_extraction` 成功后自动同步；连续性/审计报告已支持人工处理，连续性“人物出场记录未更新”可生成并应用 `append_unique` 自动修复，“人物提前出场”“设定提前揭示”可生成并应用 `set_field first_mentioned` 修复，时间线排序倒挂可生成并应用 `sort_by_chapter` 修复，应用前会创建记忆版本并写修复事件；仍缺更深的冲突合并、按人物/伏笔/认知的深度记忆治理和工作流级人工确认 |
| 知识图谱 | 62% | GraphBuilder 可从项目级记忆、大纲、章节正文和样本技巧汇总生成 `graph/graph.json`，Java 图谱读取/查询/重建/导出 API 和前端 GraphView 已打通；图谱节点已进入章节检索上下文包；Python 图谱产物已包含中心度、桥接、关系类型分析、关键路径和增量构建摘要；人物、设定、剧情、时间线、伏笔、章节大纲、章节正文和技巧节点已能生成基础 involvement / mention / related / resolved_by / advances_plot / sets_up_foreshadowing / implements_outline / uses_technique 关系；大纲顶层人物/设定/长线悬念、章节目标、已写章节正文和跨书归纳技巧已能入图；`graph_artifacts` DB 业务表按 `project_id+book_id` 唯一保存图谱 JSONB、节点/边列表、节点/边类型分布、平均度/密度统计、Top 度节点、Top 中心度/桥接节点、关系分析、关键路径、增量摘要、图谱任务和元数据，支持 `GET /graph/db` 查询、`POST /graph/sync` 手工同步，并在 `graph_build` 成功后自动同步；图谱版本快照/审计已支持手动创建、列表/详情查看和重建前自动归档，前端 GraphView 已展示高级分析摘要、DB 同步和版本抽屉；仍缺真实 LLM 关系抽取和更强交互式可视化 |
| 检索与上下文构建 | 61% | 已有 ContextBuilder，可汇入样本 chunk、单书/跨书报告、Skill、记忆、图谱和前文摘要；Python 侧本地 hybrid engine 包含关键词/BM25 风格检索、hash-vector 本地向量索引、RRF 融合、规则 rerank、retrieval plan、质量评估、引用预算和缓存指纹/失效状态；`retrieval_index` Agent 可独立重建 `indexes/bm25`、`indexes/vector`、`indexes/hybrid` 与 `retrieval_index_report.json`；Java 已有检索配置、三类索引摘要、重建任务、context pack、质量评估报告、缓存失效 API 和离线 benchmark API；`retrieval_artifacts` DB 业务表按项目唯一保存检索配置、BM25/vector/hybrid 索引摘要、重建报告、质量报告、benchmark 报告、context pack 列表、检索任务、文档计数、质量摘要字段和 benchmark 摘要指标，支持 `GET /retrieval/db` 查询、`POST /retrieval/sync` 手工同步，并在 `retrieval_index` 成功后自动同步；检索索引版本已支持手动快照、列表/详情和重建前自动归档；离线检索基准已支持固定查询集、hit rate、MRR、recall@k、质量分和逐用例明细，前端检索页已加 DB 同步、版本抽屉和 benchmark 展示。缺 LanceDB/pgvector 正式向量库、embedding/rerank 模型实调和外部 reranker |
| 前端 | 98% | 页面和 Docker 构建可用，项目列表/样本页已接真实 API；ProjectList 已支持搜索、状态/样本分组筛选、列排序、批量归档、状态下一步引导和 Dashboard 创建入口；模型配置页可管理 DB-backed profile、主模型/快速模型、默认配置和测试，API Key 以掩码状态展示；RetrievalView 可查看检索配置、索引摘要、上下文包、质量报告、重建任务和索引版本，并可触发索引重建、DB 同步、质量评估、缓存清理和版本创建；ArtifactView 可按分类查看、搜索、预览、下载、批量导出、归档、恢复、归档永久删除、差异对比、查看审计日志和执行 30 天归档保留策略，并对敏感样本预览/下载/diff/批量导出执行授权确认且传递治理角色；ProjectDetail、OutlineEditor、ChapterWriter 已接 Skill/books/outline/soul/chapter/retrieval/finalize/boundary/revisionHistory/revision/versions/reviews/restore/human-review/update 真实 API，ProjectDetail 已支持 Skill 查看、编辑、启停、质量校验、批准/驳回、历史版本恢复、历史版本对比当前、冲突提示和内置工作流启动/审批，OutlineEditor 已支持大纲/Project Soul 编辑保存、大纲审查触发与审查报告查看，章节详情已支持正文手工编辑保存；SampleManagement 已有分析状态、任务轮询、manifest/chunk/单书/跨书报告查看和下载，并提供覆盖率队列展示、失败/缺失 chunk 聚合诊断、定向修复、“校验并自动修复”、覆盖率/问题块/Manifest/分块索引/分析包导出入口；TaskCenter 已能全局查看任务、失败诊断、事件日志、retry/resume/cancel、人工确认审批和 retry policy 退避信息；MemoryView、GraphView、RetrievalView 与 ArtifactView 已接真实 API，MemoryView 已能在连续性/审计报告中应用结构化记忆修复；axios 已统一注入可配置身份 header；仍缺正式登录页、用户/组织管理和权限审计 UI |
| 测试与质量保障 | 43% | 有阶段验证脚本和本次烟测；新增记忆连续性/审计 fix 的 Python/Java 定向测试，覆盖 `append_unique`、`set_field` 与 `sort_by_chapter` 修复；新增覆盖率 DB 同步定向测试，验证失败分析文件会回写为 `FAILED` 且不会误标已处理，并验证 `coverage_check` 完成后触发结构/分析结果同步；新增任务 retry policy 定向测试，覆盖可配置最大重试次数、指数退避诊断和 retry metrics 保留；新增 LLM Gateway 定向测试，覆盖 profile fallback、缓存命中、成本估算、usage 台账、运行时 secret 引用、embedding/rerank 本地降级和实际限流等待；新增迁移治理配置守护测试，验证默认/prod/ci 均使用 Flyway + Hibernate validate；新增统一产物治理定向测试，覆盖批量导出敏感样本保护、manifest 跳过原因、bulk_export 审计统计、审计事件倒序 limit、viewer 归档拒绝审计、归档保留策略 dry-run 与实际清理；新增 MVP smoke 脚本状态断言和大纲批准边界门槛定向测试；前端固定 E2E 已覆盖任务中心与覆盖率修复入口；已有 GitHub Actions CI 基础，覆盖 Python/Java/前端三路验证、固定 smoke、Flyway 迁移校验和失败 artifacts 留存；仍缺覆盖率门槛、更多集成测试、性能样例和发布门禁 |

综合判断：**完整文档目标约完成 84%；可运行 MVP 骨架约完成 95%；样本导入这一段约完成 91%；样本分析到 Skill 沉淀链路约完成 87%；模型配置 DB 持久化与 API Key 加密、Skill Profile DB 同步、Outline Artifact DB 同步、Chapter Artifact DB 同步、Skill 管理与版本恢复/质量校验/审批、大纲/Project Soul 编辑/大纲审查/大纲边界自动补齐/章节/边界检查/独立返修/章节版本恢复/章节人工审查/章节手工编辑保存/终稿多版本/终稿记忆/记忆连续性报告自动修复/图谱/检索上下文查看、统一产物浏览、最小工作流 checkpoint/resume、样本章节/分块 DB 同步、逐块分析结果 DB 同步、覆盖率修复队列/自动修复入口和全文分析逐块断点续跑链路已开始进入可演示状态；检索侧已从“关键词上下文包”推进到“本地混合检索 + 可配置重建 + 前端管理”闭环，但还不是文档要求的生产级向量检索系统。**

## 2. 本次验证结果

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| Python 编译 | 通过 | `python -m compileall -q .` 在 `novel-system/python-services` 成功 |
| Java Docker 构建 | 通过 | `docker compose build java-backend` 成功，最新 `TaskExecutorService` 可编译 |
| Python Docker 构建 | 通过 | `docker compose build python-ai` 成功 |
| 前端 Docker 构建 | 通过 | `docker compose build frontend` 成功，仅有 Vite chunk 过大警告 |
| Python 健康检查 | 通过 | `GET http://localhost:8000/health` 返回 healthy |
| Java 应用健康检查 | 通过 | `GET http://localhost:8080/api/health` 返回 UP；`/actuator/health` 也返回 UP |
| 前端访问 | 通过 | `GET http://localhost/` 返回 200 |
| 前端生产 API 代理 | 通过 | `api.ts` 默认 `baseURL` 已由 `http://localhost:8080/api` 改为同源 `/api`，生产环境经 nginx 代理访问后端，解决浏览器 CORS 阻塞 |
| 前端浏览器路由烟测 | 通过 | 使用系统 Chrome 无头访问 `/`、`/projects/20260624_164140_41ac`、`/outline`、`/chapters`；项目详情、大纲、章节页均能渲染真实数据 |
| 模型配置 DB/加密/API/前端路由烟测 | 通过 | `/api/model-profiles` 列表/创建/详情/更新/删除、`/default`、`/{profileId}/default`、`/{profileId}/test` 可用；`codex_smoke_mock` 保持默认，默认 `model_profile_id` 可自动注入任务参数；新增 `codex_db_key_smoke` 带测试 API Key 的 profile 后，接口详情返回 `apiKey=enc:****`、`hasApiKey=true`、`apiKeyEncrypted=true`，DB 中 `main_model->>'apiKey'` 为 `enc:v1:` 前缀且不含明文；`/{profileId}/test` 返回 `passed`；前端 `/models` 返回 200 |
| Skill Profile DB 同步烟测 | 通过 | 新增 `SkillProfile` 实体、`SkillProfileRepository`、`GET /api/projects/{projectId}/skills/profile` 和 `POST /api/projects/{projectId}/skills/profile/sync`；测试项目同步后 `skill_profiles` 表存在 1 条 `20260624_164140_41ac:default`，`enabled_skills` 为 JSONB 且包含 3 个 Skill 内容快照，`skill_metadata.skillCount=3`、`enabledCount=3`，`task_overrides` 包含 `chapter_writing -> writing_skill`；临时编辑并恢复 `writing_skill` 后，DB 内容同步更新且无 smoke marker 残留 |
| Docker Java health 状态 | 通过 | healthcheck 已改用运行镜像内可用的 `wget`，`novel-java-backend` 显示 healthy |
| 样本导入烟测 | 通过 | 创建项目、上传 `.txt`、自动触发 `sample_import`，任务 `SUCCESS`，样本回写 `CHUNKED`、`totalChapters=2`、`totalChunks=3` |
| 样本章节/分块 DB 持久化烟测 | 通过 | 新增 `sample_chapters`、`sample_chunks` 实体、Repository 和同步接口；`POST /api/projects/{projectId}/samples/{sampleId}/structure/sync` 后，测试样本读回章节 3 条、分块 3 条、已处理分块 3 条 |
| mock 全文分析烟测 | 通过 | `full_text_analysis` 在 `MOCK_LLM=true` 下 3/3 chunks 成功，样本回写 `ANALYZED` |
| 逐块分析结果 DB 持久化烟测 | 通过 | 新增 `analysis_results` 实体、Repository、同步服务和查询接口；`POST /api/projects/{projectId}/analysis/samples/{sampleId}/analysis/sync` 后，测试样本读回 DB 结果 3 条、成功 3 条；`analysis`/`raw_result` 为 JSONB，`chunk_0000` 摘要可从 DB API 和 SQL 读回 |
| 覆盖率校验烟测 | 通过 | 新增 `coverage_check` Agent；`POST /analysis/samples/sample_b9cc9d79fa364ea5/coverage/check` 生成 `analysis/coverage/sample_b9cc9d79fa364ea5_coverage.json`，文本覆盖率 `1.0`、分析覆盖率 `1.0`、缺失分析块 `0`、失败块 `0` |
| mock 单书报告烟测 | 通过 | `book_summary` 成功生成 `analysis/per_book/{sampleId}_report.md` |
| mock 跨书归纳烟测 | 通过 | 双样本单书报告后执行 `cross_book_synthesis`，生成 `analysis/cross_book/cross_book_synthesis.md` 和 `technique_summary.json` |
| mock Skill 生成烟测 | 通过 | `skill_generation` 生成 `skills/local/writing_skill.md`、`outline_skill.md`、`review_skill.md` 与 `skills/enabled.yaml` |
| Java Skill API 烟测 | 通过 | `GET /api/projects/{projectId}/skills` 返回 3 个 Skill；`/skills/enabled` 返回 3 条启用配置；`/skills/writing_skill` 返回正文内容 |
| Skill 管理烟测 | 通过 | `PATCH /skills/writing_skill/config` 可调整启停/优先级/类型/scope，`PATCH /skills/writing_skill` 可保存 Markdown 内容，`/skills/conflicts` 可返回冲突检测结果；测试后已恢复原内容和配置 |
| Skill 版本恢复烟测 | 通过 | `/skills/writing_skill/versions` 返回 4 条内容版本，版本详情可读正文；`POST /versions/{versionId}/restore` 可恢复历史版本，恢复前归档当前文件并写入 `skill_restore` 报告；`/skills/enabled/versions` 可读配置版本 |
| Skill 质量校验/审批烟测 | 通过 | `POST /skills/writing_skill/quality-check` 返回 `needs_review`、`score=74` 并写入 `skill_quality_check` 报告；`POST /approve` 可写入 `approved` 和 `skill_approval` 报告；`POST /reject` 可写入 `rejected`、停用 Skill 并写入 `skill_rejection` 报告，随后已将 `review_skill` 批准恢复启用 |
| Project Soul 烟测 | 通过 | `outline_generation` 生成 `novel/soul/project_soul.md`，章节创作 prompt 可读取并注入 |
| 章节草稿烟测 | 通过 | `chapter_writing` 生成 `novel/chapters/drafts/{bookId}/volume_1/chapter_1.json`，记录使用的项目 Skills 和审查结果 |
| 大纲/章节读取 API 烟测 | 通过 | `/books/default/outline`、`/books/default/soul`、`/books/default/chapters`、`/chapters/1/1` 均能读取最新书籍产物 |
| 分析产物 API 烟测 | 通过 | `/analysis/status`、`/analysis/report`、`/samples/{sampleId}/artifacts`、`/chunks`、`/analysis`、`/book-report`、`/cross-book` 能读取 manifest、分块、逐块分析、单书和跨书报告 |
| 任务诊断 API 烟测 | 通过 | `GET /api/tasks/{taskId}/logs` 能返回任务 input/output/errors/warnings/metrics/checkpoint；`POST /retry` 已补齐 |
| 样本管理页浏览器烟测 | 通过 | `/projects/20260624_164140_41ac/samples` 能展示真实分析总览、样本分块/分析数量、最近任务；样本详情抽屉和跨书报告抽屉可打开并显示产物内容 |
| 记忆提取烟测 | 通过 | `memory_extraction` 读取章节草稿，生成项目级 `memory/world_settings.json`、6 个 `memory/*.md` 与 `memory/snapshots/book_ac7722d0_v1_c1.json` |
| Java 记忆 API 烟测 | 通过 | `/memory`、`/memory/canon`、`/memory/world-settings/list`、`/memory/snapshots/list` 能读回 Markdown 内容、JSON 条目、快照和最近任务 |
| 记忆管理页浏览器烟测 | 通过 | `/projects/20260624_164140_41ac/memory` 能展示 6 类记忆、总条目、快照数量和 Canon 条目；页面有一个 favicon 404，不影响业务渲染 |
| 图谱构建烟测 | 通过 | `graph_build` 从项目级记忆构建图谱，写入 `graph/graph.json`、`graph/book_ac7722d0_graph.json` 和 book 级兼容路径 |
| Java 图谱 API 烟测 | 通过 | `/books/{bookId}/graph`、`/graph`、`/graph/query`、`/graph/export?format=graphml` 均可用；节点 ID 查询能返回“玄灯司”组织节点 |
| 图谱页面浏览器烟测 | 通过 | `/projects/20260624_164140_41ac/graph` 能展示真实图谱统计、节点列表、轻量 SVG 网络和导出入口；页面有一个 favicon 404，不影响业务渲染 |
| 检索上下文包烟测 | 通过 | `chapter_writing` 生成第 2 章时写入 `indexes/bm25/index_summary.json` 与 `context_packs/book_ac7722d0_v1_c2.json`，任务结果返回 `context_pack_path` |
| Java Retrieval API 烟测 | 通过 | `/retrieval`、`/retrieval/context-packs`、`/retrieval/context-packs/book_ac7722d0_v1_c2` 均可读取；上下文包索引 18 个文档，包含样本 chunk、报告、Skill、记忆和图谱节点 |
| 章节上下文接入烟测 | 通过 | 第 2 章草稿 `referencedChapters=[1]`，检索上下文包包含第 1 章摘要、Canon、玄灯司图谱节点和 Top 8 检索结果 |
| 章节终稿发布烟测 | 通过 | `POST /api/projects/{projectId}/books/{bookId}/chapters/1/2/finalize` 写入 `novel/chapters/final/.../chapter_2.json`，章节状态变为 `finalized`；覆盖已有终稿时会归档旧终稿、递增版本并写入 `chapter_finalize` 报告 |
| 终稿自动记忆摄取烟测 | 通过 | 发布终稿后自动创建 `memory_extraction` 任务 `task_f7079a0dd257468a` 并成功生成 `memory/snapshots/book_ac7722d0_v1_c2.json` |
| 章节边界检查烟测 | 通过 | `chapter_writing` 已生成 `boundary_check`；第 3 章返回 `boundary_passed=true`、2 条 warning；章节详情 API 可读 `boundaryCheck` |
| 后续章纲保护烟测 | 通过 | 重新生成第 1 章后，`boundaryCheck.futureProtection.protectedContent` 自动包含第 2/3 章保护项，前端章节页可展示边界结果 |
| 章节自动返修烟测 | 通过 | 临时给第 3 章注入 `must_not_write=冲突前置` 后执行 `review_iterations=3`，任务 `task_cf3f6b28a5474ef1` 返回 `PARTIAL`、`revision_count=2`、`boundary_blocking_errors=1`；章节详情 API 可读 `revisionHistory`。mock LLM 未真正改掉禁写词，测试后已恢复原大纲和原第 3 章草稿 |
| 独立 RevisionAgent 烟测 | 通过 | `chapter_revision` 已注册；Java `POST /revision` 触发任务 `task_2d59c335759049a7` 成功，当前第 3 章升到 `version=3`、`revisionHistory=2`，并写入 `novel/chapters/versions/...chapter_3_v2_*.json` 与 `novel/reviews/...chapter_3_revision_*.json`；`/versions`、`/reviews` API 和前端章节页入口已可用 |
| 章节历史版本恢复烟测 | 通过 | Java `POST /versions/{versionId}/restore` 可把历史快照恢复为新草稿或新终稿；恢复前自动归档当前目标阶段版本并写入 `chapter_restore` 报告；前端历史版本列表已拆分“恢复为草稿”和“恢复为终稿”操作 |
| 终稿多版本状态机烟测 | 通过 | 第 2 章终稿覆盖发布从 `v1` 升到 `v2`，产生 `before_finalize_overwrite` 快照和 `chapter_finalize` 报告；随后将历史快照恢复为终稿，当前终稿升到 `v4`，`finalizedAt=restoredAt`，并产生新的终稿快照和 `chapter_restore` 报告 |
| 章节人工审查烟测 | 通过 | Java `POST /review` 和文档简写 `POST /api/projects/{projectId}/chapters/{chapterNumber}/review` 均可写入 `approved/needs_revision/rejected` 决策；章节 JSON 会记录人工反馈，`novel/reviews` 会写入 `chapter_human_review` 报告；前端章节页已有人工审查弹窗和历史展示 |
| 章节手工编辑保存烟测 | 通过 | Java `PATCH /books/{bookId}/chapters/{volume}/{chapter}` 和文档简写 `PATCH /api/projects/{projectId}/chapters/{chapterNumber}` 均可更新标题/正文；保存前自动写 `before_manual_edit` 版本快照，章节状态变为 `manual_edited`，并写入 `chapter_manual_edit` 报告；前端章节详情抽屉已支持编辑/保存/取消 |
| 大纲/Project Soul 编辑保存烟测 | 通过 | Java `PATCH /books/{bookId}/outline` 和文档简写 `PATCH /api/projects/{projectId}/outline` 均可保存完整大纲 JSON 和 Project Soul；保存前自动写 `before_outline_edit` 快照，Project Soul 也会单独归档，并写入 `outline_manual_edit` 报告；前端 OutlineEditor 已支持编辑/保存 |
| 大纲审查烟测 | 通过 | `outline_review` 已注册为 `OutlineReviewAgent`；Java 文档兼容入口 `POST /api/projects/{projectId}/outline/review` 触发任务 `task_b0badab7e17e4dce` 成功；报告写入 `novel/reviews/book_ac7722d0/outline/outline_review_20260624204358548583.json`，返回 `score=73`、`status=needs_revision`，指出 3 个章节缺少边界字段 |
| 大纲边界自动补齐烟测 | 通过 | `POST /api/projects/{projectId}/outline/review` 传 `auto_fix=true` 可归档旧大纲并补齐章节边界字段；`task_84ac8f36e7d4463c` 将测试大纲从 `chaptersWithBoundary=0/3` 修复为 `3/3`，写入 `outline_boundary_fix` 报告；`task_854ecb76c48b4322` 传 `overwrite_boundary=true` 后评分 `100`、0 错误、0 警告 |
| 检索索引重建/API/UI 烟测 | 通过 | 新增 `retrieval_index` Agent，`/api/agents/list` 可见该 Agent；`PATCH /api/projects/{projectId}/retrieval/config` 可写入 `indexes/retrieval_config.json`；`POST /api/projects/{projectId}/retrieval/rebuild` 触发任务 `task_d027554909574c0f` 成功，产出 18 个检索文档、`indexes/bm25/index_summary.json`、`indexes/vector/index_summary.json`、`indexes/hybrid/index_summary.json` 和 `indexes/retrieval_index_report.json`；`/retrieval/indexes/vector` 返回 `document_count=18`，`/retrieval/indexes/hybrid` 返回 `vector_count=18`、`returned_count=9`；前端 `/projects/20260624_164140_41ac/retrieval` 返回 200 |
| 统一产物管理 API/UI 烟测 | 通过 | 新增 `/api/projects/{projectId}/artifacts`、`/artifacts/list`、`/artifacts/view`、`/artifacts/download`；测试项目返回 8 个分类、合计 135 个产物，`indexes` 分类 8 个产物；`artifacts/view?path=indexes/hybrid/index_summary.json` 可返回文本内容和结构化 JSON，下载返回 200；`../config/model_profiles.json` 路径穿越请求返回 400；前端 `/projects/20260624_164140_41ac/artifacts` 返回 200 |
| 最小工作流/checkpoint/resume 烟测 | 通过 | 新增 Python `workflow` Agent，`/api/agents/list` 返回 15 个 Agent；新增 Java `POST /api/tasks/{taskId}/resume`。`human_review_gate` 工作流第一次执行任务 `task_7f622d8a5f604ec6` 返回 `PARTIAL`，暂停在 `wait_for_approval` 并写入 checkpoint；随后 `/resume` 带 `approved=true` 创建任务 `task_f19f16f953c148c2`，恢复后执行后续 `retrieval_index_after_approval` 节点并返回 `SUCCESS`，`resume_count=1` |
| 全文分析逐块 checkpoint/resume 烟测 | 通过 | `full_text_analysis` 每个 chunk 后和任务结束时写入 `checkpoints/{taskId}_full_text_analysis_{sampleId}_*.json`；任务 `task_52e0b58b3cd54aa1` 返回 `SUCCESS`、`checkpointRef=/workspace/projects/.../task_52e0b58b3cd54aa1_full_text_analysis_sample_b9cc9d79fa364ea5_*.json`，容器内文件存在；`POST /api/tasks/task_52e0b58b3cd54aa1/resume` 创建任务 `task_62942fb524a54122`，从该 checkpoint 恢复后跳过 3 个已完成 chunk，返回 `SUCCESS`、`ANALYSIS_RESUMED_FROM_CHECKPOINT` |

烟测细节：

- 测试项目：`20260624_155609_965d`
- 测试样本：`sample_749b0419d2424fc6`
- 自动任务：`task_dfa5c019c65e41e8`
- 任务状态：`SUCCESS`
- 输出：`samples/normalized/...`、`samples/chunks/...`、`samples/manifests/...`
- 后续已复测：`TextNormalizer` 会移除 UTF-8 BOM，manifest 与 normalized 文本首字符正常。

新增 Skill 链路烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试样本：`sample_b9cc9d79fa364ea5`、`sample_0731be8ce2944871`
- 验证链路：上传两个样本 -> 自动 `sample_import` -> 两次 `full_text_analysis` -> 两次 `book_summary` -> `cross_book_synthesis` -> `skill_generation`
- 生成文件：`skills/local/writing_skill.md`、`skills/local/outline_skill.md`、`skills/local/review_skill.md`、`skills/enabled.yaml`
- API 验证：Skill 列表 3 条、enabled 配置 3 条、`writing_skill` 内容长度 2822 字符。

新增 Skill 管理烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试 Skill：`writing_skill`
- 配置编辑：`PATCH /api/projects/20260624_164140_41ac/skills/writing_skill/config` 临时改为 `enabled=false`、`priority=101`，接口返回更新后的配置。
- 内容编辑：`PATCH /api/projects/20260624_164140_41ac/skills/writing_skill` 临时追加 `<!-- skill edit smoke marker -->`，再次读取可观察到 marker。
- 恢复验证：测试后把内容、`enabled=true`、`priority=100`、`type=writing`、`scope=chapter_writing,style_guidance` 全部恢复，最终 `contentRestored=true`。
- 冲突检测：`GET /api/projects/20260624_164140_41ac/skills/conflicts` 返回 `conflictCount=0`。
- 归档验证：运行容器 `/workspace/projects/20260624_164140_41ac/skills/versions` 下产生 `writing_skill` 内容快照与 `enabled.yaml` 配置快照；`novel/reviews/skills` 下产生 `skill_snapshot`、`skill_manual_edit`、`skill_config_snapshot`、`skill_config_edit` 报告。
- 前端验证：`ProjectDetail` 的 Skills 标签页已支持查看、编辑、启停和冲突提示；`docker compose build frontend` 通过。

新增模型配置 DB / API Key 加密烟测细节：

- Java 改动：新增 `ModelProfile` 实体与 `ModelProfileRepository`，`ModelProfileService` 以 DB `model_profiles` 表为权威存储；旧 `/workspace/config/model_profiles.json` 仅作为初始化来源和 Python 运行时兼容导出。
- 加密策略：DB 中 API Key 使用 Java JCE AES-GCM 写入，格式为 `enc:v1:<iv>:<ciphertext>`；接口响应统一脱敏为 `enc:****`，并返回 `hasApiKey=true`、`apiKeyEncrypted=true`。
- 烟测 profile：`codex_db_key_smoke`，写入测试 key `sk-test-1234567890abcdef`。
- API 验证：`GET /api/model-profiles/codex_db_key_smoke` 返回 `apiKey=enc:****`、`hasApiKey=true`、`apiKeyEncrypted=true`；`POST /api/model-profiles/codex_db_key_smoke/test` 返回 `passed`。
- DB 验证：`select main_model->>'apiKey' from model_profiles where profile_id='codex_db_key_smoke'` 返回 `enc:v1:` 前缀，且不包含测试 key 明文。
- 默认配置验证：`codex_smoke_mock` 仍为默认 profile，`default_mock` 和 `codex_db_key_smoke` 均保持启用但非默认。
- 兼容导出说明：`/workspace/config/model_profiles.json` 仍作为 Python `LLMClient` 的运行时兼容配置文件，但 API Key 已改为 `apiKeyRef` 引用，不再在该文件中落明文；实际 key 单独写入 `/workspace/config/model_profile_secrets.json`，Python 运行时会解析该 secrets 文件。后续应升级为生产级 secrets provider/轮换/权限审计，减少本地 secrets 文件暴露面。
- 构建/健康验证：`python -m compileall -q .` 通过；`docker compose build java-backend` 通过；重启后 `GET /api/health` 返回 `UP`。

新增 Skill 版本恢复烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试 Skill：`writing_skill`
- 版本列表：`GET /api/projects/20260624_164140_41ac/skills/writing_skill/versions` 返回 4 条内容版本。
- 版本详情：`GET /skills/writing_skill/versions/writing_skill_20260624203104630` 可读回版本正文内容。
- 恢复验证：临时追加 `<!-- skill restore smoke marker -->` 后，通过 `POST /skills/writing_skill/versions/writing_skill_20260624203012691/restore` 恢复原版本，最终正文长度恢复为 2822 且 marker 移除。
- 审计产物：恢复前自动写 `skills/versions/writing_skill/writing_skill_20260624203104630.md` 快照；恢复报告写入 `novel/reviews/skills/writing_skill_skill_restore_20260624203104632.json`。
- 配置版本：`GET /api/projects/20260624_164140_41ac/skills/enabled/versions` 返回 2 条 `enabled.yaml` 配置快照。
- 前端验证：`ProjectDetail` 的 Skills 操作区新增“版本”入口，历史版本弹窗可列出版本并触发恢复；`docker compose build frontend` 通过。

新增覆盖率校验烟测细节：

- Python Agent：`coverage_check` 已注册，agent class 为 `CoverageCheckAgent`；当前 `/api/agents/list` 总数为 15。
- Java 触发入口：`POST /api/projects/20260624_164140_41ac/analysis/samples/sample_b9cc9d79fa364ea5/coverage/check`
- 覆盖率任务：`task_d2fe567471d2444a`
- 任务状态：`SUCCESS`
- 报告文件：`analysis/coverage/sample_b9cc9d79fa364ea5_coverage.json`
- 报告结果：`status=passed`、`textCoverage.coverageRatio=1.0`、`analysisCoverage.coverageRatio=1.0`、`missingAnalysisCount=0`、`failedChunkCount=0`。
- API 验证：`GET /api/projects/20260624_164140_41ac/analysis/samples/sample_b9cc9d79fa364ea5/coverage` 能读回完整报告；`/artifacts` 聚合接口返回 `hasCoverageReport=true`、`coverageReportPath=analysis/coverage/sample_b9cc9d79fa364ea5_coverage.json` 和 camelCase `coverageReport`。
- 修复队列：`coverage_check` 可为缺失/失败 chunk 写入 `analysis/repairs/{sampleId}_repair_queue.json`，并在 `auto_repair=true` 时调用 `analysis_repair` 执行定向补偿。
- DB 回写：`coverage_check` 任务成功或部分成功后，Java 会同步 `sample_chunks` 与 `analysis_results`；错误分析文件会落为 `analysis_results.status=FAILED`，对应 chunk 不再被误标为 `processed=true`。
- 前端验证：`SampleManagement` 新增“校验”“自动修复”按钮和“覆盖率”详情页签，可展示文本覆盖率、分析覆盖率、缺失分析块、失败分析块、修复队列和完整 JSON，并可触发“校验并自动修复”；`npm run build` 与 Playwright smoke 覆盖该入口。

新增 Project Soul / 章节烟测细节：

- 测试项目：`20260624_164140_41ac`
- 生成书籍：`book_ac7722d0`
- Project Soul：`novel/soul/project_soul.md`
- 大纲文件：`novel/outline/book_ac7722d0_outline.json`
- 章节草稿：`novel/chapters/drafts/book_ac7722d0/volume_1/chapter_1.json`
- 章节验证：`used_skills=["outline_skill","writing_skill","review_skill"]`，`review_status=reviewed`，`quality_score=79.0`。
- Java API 验证：`GET /api/projects/20260624_164140_41ac/books/default/outline` 返回 `book_ac7722d0`、Project Soul 内容 1250 字符；章节列表 1 条；章节详情正文 128 字符。

新增前端真实 API 接入烟测细节：

- `ProjectDetail`：能读取项目详情、样本、任务、Skill 列表、书籍列表和 Project Soul，并展示 Skills/大纲/章节入口。
- `OutlineEditor`：能读取书籍列表和最新大纲，显示书名、类型、核心概念、世界观、主要冲突等真实数据。
- `ChapterWriter`：能读取最新书籍章节列表，显示第 1 卷第 1 章《雪夜玉牌》、字数、质量评分和状态。
- 浏览器验证发现并修复生产环境 API CORS 问题：前端默认 API 地址改为 `/api` 后，项目详情、大纲、章节路由不再出现跨域失败。

新增记忆系统烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试书籍：`book_ac7722d0`
- 测试章节：`chapter_fb25967e`，第 1 卷第 1 章《雪夜玉牌》
- 记忆任务：`task_7e5041452ed747d5`
- 任务状态：`SUCCESS`
- 生成产物：`memory/world_settings.json`、`memory/canon.md`、`memory/characters.md`、`memory/foreshadowing.md`、`memory/timeline.md`、`memory/relationships.md`、`memory/cognition.md`、`memory/snapshots/book_ac7722d0_v1_c1.json`
- API 验证：`canon` 记忆读回 1 条设定“玄灯司”，快照列表读回 `book_ac7722d0_v1_c1`。
- 前端验证：`MemoryView` 能显示 6 类记忆、总条目 1、快照 1、记忆任务 1。

新增记忆数据库持久化（本次代码核对新增，未跑烟测，仅静态核对）：

- 新增 `MemoryArtifact` 实体、`MemoryArtifactRepository` 和 `MemoryArtifactService`，`memory_artifacts` 表按 `project_id` 唯一保存记忆聚合快照。
- 字段包含：状态、人物/世界观/剧情/伏笔/时间线条目计数、Markdown 数量、快照数量、记忆目录路径、book 级记忆路径、最新快照 id/路径、6 类记忆 Markdown JSONB、记忆 JSON JSONB、最新快照 JSONB、快照列表 JSONB、最近记忆任务 JSONB 和元数据 JSONB。
- 新增 `GET /api/projects/{projectId}/memory/db`（从 DB 读取聚合记忆）和 `POST /api/projects/{projectId}/memory/sync`（从 workspace 同步入库），与原有只读 `MemoryService` 接口并存。
- `TaskExecutorService` 已在 `memory_extraction` 任务成功后自动调用 `memoryArtifactService.syncMemoryFromWorkspace(...)`，同步失败仅告警不影响主流程。
- 前端 `MemoryView` 已新增“同步记忆数据库”按钮，调用 `memoryApi.syncDb`（`/memory/sync`），`api.ts` 已封装 `getFromDb`/`syncDb`。
- 限制说明：本项原本尚未跑实际烟测验证 DB 落库内容；2026-06-27 已补连续性报告确定性修复验证，覆盖 `append_unique` 和 `set_field first_mentioned`；2026-06-28 已补 `sort_by_chapter` 时间线排序修复的 Java 定向验证。深度冲突合并、按人物/伏笔/认知的深度治理和工作流级人工确认仍缺。

新增记忆连续性自动修复验证细节（2026-06-27）：

- Python `MemoryQueryAgent` 已在“人物出场记录未更新”连续性问题中生成 `fix.action=append_unique`，目标文件为 `characters.json`、字段为 `appearances`，并带人物匹配条件和排序标记。
- Python `MemoryQueryAgent` 已在“人物提前出场”“设定提前揭示”连续性问题中生成 `fix.action=set_field`，可把 `characters.json` / `world_settings.json` 的 `first_mentioned` 调整为当前章节。
- Python `MemoryQueryAgent` 已在“时间线排序倒挂”审计问题中生成 `fix.action=sort_by_chapter`，Java `MemoryService` 可按章节重排 `timeline.json`。
- Java `MemoryService` 新增连续性报告 fix 入口，复用审计报告自动修复机制；应用前创建 `before_memory_continuity_fix` / `before_memory_audit_fix` 版本，应用后把 issue 标记为 `resolved/fix_applied`，并写入 `memory_continuity_fix` 或 `memory_audit_fix` 事件。
- 前端 `MemoryView` 的报告问题表格已统一支持连续性/审计报告的“应用修复”按钮。
- 验证命令：`python -m pytest tests/test_memory_continuity.py`、Docker Maven `-Dtest=MemoryServiceTest test`、Docker Maven `mvn -B test`、前端 `npm run build`、`npm run e2e:ci`、`git diff --check` 均通过。

新增知识图谱数据库持久化（2026-06-26 本次实现，未跑烟测，仅静态核对）：

- 新增 `GraphArtifact` 实体、`GraphArtifactRepository` 和 `GraphArtifactDbService`，`graph_artifacts` 表按 `project_id+book_id` 唯一保存图谱聚合快照（沿用 `memory_artifacts` 的实现模式，DB 服务仅依赖 `GraphArtifactRepository`+`TaskRepository`，避免与 `TaskExecutorService` 的循环依赖）。
- 字段包含：状态、graph_id、节点/边数量、人物/地点/组织/物品节点计数、平均度、密度、图谱路径、完整图谱 JSONB、节点列表 JSONB、边列表 JSONB、统计 JSONB、节点/边类型分布 JSONB、Top 度节点 JSONB、最近图谱任务 JSONB 和元数据 JSONB。
- `GraphArtifactDbService.syncGraphFromWorkspace` 会按 `{bookId}_graph.json` -> `graph.json` -> `books/{bookId}/graph/knowledge_graph.json` 的顺序解析图谱文件，重算节点/边类型分布、度统计、Top 度节点，并写回 DB。
- 新增端点：项目级 `GET /api/projects/{projectId}/graph/db`、`POST /api/projects/{projectId}/graph/sync`、`GET /api/projects/{projectId}/graphs/db`，以及 book 级 `GET /api/projects/{projectId}/books/{bookId}/graph/db`、`POST /api/projects/{projectId}/books/{bookId}/graph/sync`；与原有基于文件的只读 `GraphArtifactService`（`/graph`、`/graph/query`、`/graph/rebuild`、`/graph/export`）并存。
- `TaskExecutorService` 已在 `graph_build` 任务成功后自动调用 `graphArtifactDbService.syncGraphFromWorkspace(...)`，同步失败仅告警不影响主流程。
- 前端 `GraphView` 已新增“同步图谱数据库”按钮，调用 `graphApi.syncDb`（book 级 `/graph/sync`），`api.ts` 已封装 book 级 `getDb`/`syncDb`。
- 表结构已纳入 Flyway `V1__init_application_schema.sql`，默认启动由 Hibernate `validate` 校验迁移结果，与 `memory_artifacts` 等业务表一致。
- 限制说明：本项尚未跑实际 Docker 构建与烟测验证（当前 VM 仅有 Java 11、无 Maven/Docker，项目需 Java 17），仅做静态代码核对（实体 setter 与字段、Repository 方法、Controller 注入、import 与括号配平均已核对）；后续已补高级中心度/路径、增量图谱和伏笔基础关系抽取，仍缺从章节正文/大纲/样本技巧中抽取更多实体关系。

新增检索数据库持久化（2026-06-26 初版实现，2026-06-27 补齐质量报告与 benchmark 报告 DB 同步并通过定向测试）：

- 新增 `RetrievalArtifact` 实体、`RetrievalArtifactRepository` 和 `RetrievalArtifactDbService`，`retrieval_artifacts` 表按 `project_id` 唯一保存检索聚合快照（沿用 `memory_artifacts`/`graph_artifacts` 模式，DB 服务仅依赖 `RetrievalArtifactRepository`+`TaskRepository`，避免与 `TaskExecutorService` 的循环依赖）。
- 字段包含：状态、BM25/vector/hybrid 文档数、上下文包数量、检索配置路径、重建报告路径、质量报告路径、benchmark 报告路径、检索配置 JSONB、BM25/vector/hybrid 三类索引摘要 JSONB、重建报告 JSONB、质量报告 JSONB、benchmark 报告 JSONB、上下文包列表 JSONB、最近检索任务 JSONB 和元数据 JSONB。`documentCount` 兼容 `document_count`/`vector_count`/`returned_count`/`total_documents` 等字段名。
- `RetrievalArtifactDbService.syncRetrievalFromWorkspace` 会读取 `indexes/retrieval_config.json`、`indexes/{bm25,vector,hybrid}/index_summary.json`、`indexes/retrieval_index_report.json`、`indexes/retrieval_quality_report.json`、`indexes/retrieval_benchmark_report.json` 和 `indexes/bm25/context_packs/*.json`，汇总后写回 DB；DB 响应和列表摘要已返回 `qualityScore`、`qualityStatus`、`benchmarkStatus`、`benchmarkCaseCount`、`benchmarkPassedCount`、`benchmarkHitRate`、`benchmarkMeanReciprocalRank`，元数据含 `latestQualityReport` 与 `latestBenchmarkReport`。
- 新增端点：`GET /api/projects/{projectId}/retrieval/db`、`POST /api/projects/{projectId}/retrieval/sync`、`GET /api/projects/{projectId}/retrieval/list/db`；与原有基于文件的只读 `RetrievalArtifactService`（`/retrieval`、`/retrieval/indexes`、`/retrieval/config`、`/retrieval/rebuild`、`/retrieval/context-packs`）并存。
- `TaskExecutorService` 已在 `retrieval_index` 任务成功后自动调用 `retrievalArtifactDbService.syncRetrievalFromWorkspace(...)`，同步失败仅告警不影响主流程。
- 前端 `RetrievalView` 已新增“同步检索数据库”按钮，调用 `retrievalApi.syncDb`（`/retrieval/sync`），`api.ts` 已封装 `getDb`/`syncDb`。
- 表结构已纳入 Flyway 迁移：`V1__init_application_schema.sql` 创建基础表，`V3__add_retrieval_quality_report.sql` 补齐质量报告字段，`V4__add_retrieval_benchmark_report.sql` 补齐 benchmark 报告字段。
- 限制说明：检索质量评估、引用预算、缓存状态/失效、质量报告 DB 同步、benchmark 报告 DB 同步、索引版本治理和离线检索基准已有代码与定向测试覆盖；正式向量库（pgvector/LanceDB）、真实 embedding/rerank 和外部 reranker 仍缺。

新增知识图谱烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试书籍：`book_ac7722d0`
- 图谱任务：`task_f4a9819b717e4217`
- 任务状态：`SUCCESS`
- 生成产物：`graph/graph.json`、`graph/book_ac7722d0_graph.json`、`books/book_ac7722d0/graph/knowledge_graph.json`
- API 验证：图谱包含 1 个 organization 节点“玄灯司”；`GET /api/projects/{projectId}/graph` 与 `GET /api/projects/{projectId}/books/{bookId}/graph` 均能读取；GraphML 导出返回 200。
- 前端验证：`GraphView` 能显示节点总数 1、组织节点 1、图谱路径、节点“玄灯司”和轻量 SVG 网络布局。

新增检索上下文烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试书籍：`book_ac7722d0`
- 章节任务：`task_cc37c78b34084788`
- 任务状态：`SUCCESS`
- 新生成章节：`chapter_640a87ac`，第 1 卷第 2 章《卷宗疑云》
- 生成产物：`indexes/bm25/index_summary.json`、`indexes/bm25/context_packs/book_ac7722d0_v1_c2.json`
- 任务结果：返回 `context_pack_path=/workspace/projects/20260624_164140_41ac/indexes/bm25/context_packs/book_ac7722d0_v1_c2.json`
- 索引统计：共 18 个文档，其中样本 chunk 6 个、单书报告 2 个、跨书报告 1 个、Skill 3 个、记忆文件 6 个。
- 上下文内容：包含第 1 章摘要、Canon/记忆、图谱节点“玄灯司”、Top 8 关键词检索结果和写作使用原则。
- API 验证：`GET /api/projects/20260624_164140_41ac/retrieval`、`/retrieval/context-packs`、`/retrieval/context-packs/book_ac7722d0_v1_c2` 均可读回上下文包。

新增章节终稿与记忆摄取烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试书籍：`book_ac7722d0`
- 发布章节：第 1 卷第 2 章《卷宗疑云》
- 发布接口：`POST /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/2/finalize`
- 兼容接口：`POST /api/projects/{projectId}/chapters/{chapterNumber}/finalize` 已按文档示例补齐，默认使用最新书籍和第 1 卷。
- 终稿产物：`novel/chapters/final/book_ac7722d0/volume_1/chapter_2.json` 与同名 `.txt`
- 发布结果：章节列表中第 2 章 `status=finalized`、`stage=final`、`isFinal=true`，且不会与草稿重复显示。
- 自动记忆任务：`task_f7079a0dd257468a`
- 任务状态：`SUCCESS`
- 记忆产物：`memory/snapshots/book_ac7722d0_v1_c2.json`，快照列表从 1 条增加为 2 条。

新增章节边界控制烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试书籍：`book_ac7722d0`
- 第 3 章边界任务：`task_43f34fb54a994a1a`
- 第 3 章结果：`boundary_passed=true`、`boundary_blocking_errors=0`、`boundary_warnings=2`
- 第 3 章详情 API：`GET /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3` 可读回 `boundaryCheck.boundaryControl`、`blockingErrors`、`warnings`、`info`。
- 第 1 章保护任务：`task_24a4e12f0637460e`
- 第 1 章后续保护：`futureProtection.protectedContent` 自动包含第 2 章和第 3 章核心事件，保护级别分别为 `critical`、`high`。
- 前端验证：`GET http://localhost/projects/20260624_164140_41ac/chapters` 返回 200，章节页构建已包含边界列和边界检查详情卡片。

新增章节自动返修烟测细节：

- 测试项目：`20260624_164140_41ac`
- 测试书籍：`book_ac7722d0`
- 测试方法：临时备份并修改 `novel/outline/book_ac7722d0_outline.json`，给第 3 章增加 `must_not_write=["冲突前置"]`，触发边界阻塞；测试后已恢复原大纲和原第 3 章草稿。
- 返修任务：`task_cf3f6b28a5474ef1`
- 请求参数：`chapter_writing`，第 1 卷第 3 章，`auto_review=true`，`review_iterations=3`
- 任务结果：`PARTIAL`、`revision_count=2`、`boundary_passed=false`、`boundary_blocking_errors=1`、`boundary_warnings=2`
- 任务指标：`llm_calls=6`，说明生成、审查和两轮返修调用均被执行。
- 章节产物验证：测试过程中的章节 JSON 写入 `revision_history` 2 条，`version=3`，每条记录包含 `iteration`、`trigger_types=["forbidden_content"]`、`word_count_before/after` 和阻塞问题详情。
- API 验证：`GET /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3` 能读回 `revisionHistory`；前端 `ChapterWriter` 已有“返修”列和“返修历史”卡片字段。
- 限制说明：mock LLM 固定返回相同文本，所以返修循环能验证“发现问题 -> 触发返修 -> 记录历史”，但不能证明真实模型一定能修复问题；工作流级人工确认仍未完成。

新增独立 RevisionAgent / 版本归档烟测细节：

- Python Agent：`chapter_revision` 已注册，agent class 为 `RevisionAgent`；当前 `/api/agents/list` 总数为 14。
- Java 返修入口：`POST /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3/revision`
- 返修任务：`task_2d59c335759049a7`
- 任务状态：`SUCCESS`
- 任务结果：`version=3`、`revision_count=2`、`new_revision_count=1`、`boundary_passed=true`、`boundary_blocking_errors=0`
- 章节详情：`GET /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3` 返回 `version=3`、`reviewStatus=reviewed`、`revisionHistory` 2 条。
- 版本 API：`GET /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3/versions` 返回版本快照，返修后最新路径为 `novel/chapters/versions/book_ac7722d0/volume_1/chapter_3_v2_20260624191324.json`，`archivedAt=2026-06-24T19:13:24.266244`。
- 审查/返修归档 API：`GET /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3/reviews` 返回 `chapter_revision` 报告，最新报告显示 `versionBefore=2`、`versionAfter=3`，包含用户返修要求、边界检查前后结果和本轮问题清单。
- 前端验证：`ChapterWriter` 增加“返修”按钮、返修弹窗、历史版本列表和返修/审查报告卡片；`docker compose build frontend` 通过。
- 限制说明：工作流级人工确认和审批状态机仍未完成。

新增章节历史版本恢复烟测细节：

- Java 恢复入口：`POST /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3/versions/chapter_3_v2_20260624191324/restore`
- 请求参数：`targetStage=draft`、`createVersionSnapshot=true`
- 恢复结果：第 3 章恢复为草稿，当前章节 `version=4`、`status=completed`、`stage=draft`、`reviewStatus=restored`。
- 恢复前快照：自动写入 `novel/chapters/versions/book_ac7722d0/volume_1/chapter_3_v3_20260624192811121.json`。
- 恢复报告：自动写入 `novel/reviews/book_ac7722d0/volume_1/chapter_3_restore_20260624192811151.json`，报告显示 `versionBefore=3`、`versionAfter=4`、`restoredFromVersionId=chapter_3_v2_20260624191324`。
- API 验证：`/versions` 从 2 条增加到 3 条；`/reviews` 返回最新 `reviewType=chapter_restore` 报告；章节详情可读 `restoredAt`、`restoredFromVersionId`、`previousSnapshotPath`。
- 前端验证：`ChapterWriter` 历史版本列表新增“恢复为草稿”和“恢复为终稿”按钮，恢复时默认归档当前目标阶段版本；`docker compose build frontend` 通过。

新增终稿多版本状态机烟测细节：

- Java 发布入口：`POST /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/2/finalize`
- 覆盖发布参数：`triggerMemoryExtraction=false`、`overwrite=true`、`createVersionSnapshot=true`、`finalizer=smoke-test`。
- 覆盖发布结果：第 2 章终稿从 `versionBefore=1` 升到 `versionAfter=2`，返回 `previousFinalSnapshotPath=novel/chapters/versions/book_ac7722d0/volume_1/chapter_2_v1_20260624211017021.json`。
- 终稿审计报告：写入 `novel/reviews/book_ac7722d0/volume_1/chapter_2_finalize_20260624211017031.json`，`/reviews` 可读 `reviewType=chapter_finalize`、`versionBefore=1`、`versionAfter=2`。
- 终稿恢复入口：`POST /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/2/versions/chapter_2_v1_20260624211017021/restore`
- 终稿恢复参数：`targetStage=final`、`createVersionSnapshot=true`。
- 终稿恢复结果：历史快照恢复为新终稿，当前终稿升到 `version=4`，返回 `previousSnapshotPath=novel/chapters/versions/book_ac7722d0/volume_1/chapter_2_v3_20260624211247170.json` 和 `reportPath=novel/reviews/book_ac7722d0/volume_1/chapter_2_restore_20260624211247189.json`。
- 元数据验证：恢复为终稿时 `stage=final`、`status=finalized`、`finalizedAt=restoredAt=2026-06-24T21:12:47.187934972`。
- 构建验证：`docker compose build java-backend` 和 `docker compose build frontend` 均通过；前端仅保留 Vite chunk 过大警告。

新增章节人工审查 / 人工确认烟测细节：

- Java 精确入口：`POST /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3/review`
- Java 文档兼容入口：`POST /api/projects/20260624_164140_41ac/chapters/3/review`
- 支持决策：`approved`、`needs_revision`、`rejected`，并兼容 `approve/request_changes/reject` 等别名。
- `needs_revision` 烟测：章节详情返回 `reviewStatus=needs_revision`、`humanReviewStatus=needs_revision`、`needsRevision=true`，`/reviews` 新增 `chapter_3_human_review_20260624193659546`。
- `approved` 烟测：通过简写入口把章节重新批准，章节详情返回 `reviewStatus=approved`、`humanReviewStatus=approved`、`humanReviewer=codex-smoke`、`humanFeedback=manual approval smoke test`、`needsRevision=false`。
- 审查归档：`GET /books/book_ac7722d0/chapters/1/3/reviews` 返回最新 `reviewType=chapter_human_review` 报告，报告记录 `decision`、`reviewer`、`feedback`、`chapterPath`、`versionBefore=4`、`versionAfter=4`。
- 前端验证：`ChapterWriter` 章节列表新增审查状态列；详情抽屉新增“批准 / 要求修改 / 驳回”操作、人工审查弹窗和人工审查历史；`docker compose build frontend` 通过。

新增章节手工编辑 / 保存烟测细节：

- Java 精确入口：`PATCH /api/projects/20260624_164140_41ac/books/book_ac7722d0/chapters/1/3`
- Java 文档兼容入口：`PATCH /api/projects/20260624_164140_41ac/chapters/3`
- 支持字段：`chapterTitle`、`content`、`editNote`、`editor`、`sourceStage`、`createVersionSnapshot`、`incrementVersion`。
- 精确入口烟测：第 3 章从 `version=4` 升到 `version=5`，返回 `snapshotPath=novel/chapters/versions/book_ac7722d0/volume_1/chapter_3_v4_20260624194927443.json`，报告为 `chapter_3_manual_edit_20260624194927460.json`。
- 简写入口烟测：第 3 章从 `version=5` 升到 `version=6`，章节详情返回 `reviewStatus=manual_edited`、`humanReviewStatus=manual_edited`、`editedBy=codex-smoke`、`manualEditHistoryCount=2`。
- 版本归档：`/versions` 最新快照为 `chapter_3_v5_20260624195228716`，`archiveReason=before_manual_edit`，`sourcePath=novel/chapters/drafts/book_ac7722d0/volume_1/chapter_3.json`。
- 审查归档：`/reviews` 最新报告 `reviewType=chapter_manual_edit`，路径为 `novel/reviews/book_ac7722d0/volume_1/chapter_3_manual_edit_20260624195228733.json`。
- 前端验证：`ChapterWriter` 详情抽屉的“正文内容”卡片新增编辑、保存、取消按钮；编辑态可修改章节标题、编辑说明和正文；保存后刷新章节详情、版本列表、审查报告和章节列表；`docker compose build frontend` 通过。

新增大纲 / Project Soul 编辑保存烟测细节：

- Java 精确入口：`PATCH /api/projects/20260624_164140_41ac/books/book_ac7722d0/outline`
- Java 文档兼容入口：`PATCH /api/projects/20260624_164140_41ac/outline`
- 支持字段：`outline`（完整大纲对象或 JSON 字符串）、`projectSoul`、`editNote`、`editor`、`createVersionSnapshot`。
- 保存结果：接口返回 `updatedPath=novel/outline/book_ac7722d0_outline.json`、`snapshotPath=novel/outline/versions/book_ac7722d0/book_ac7722d0_outline_20260624200409299.json`、`soulSnapshotPath=novel/soul/versions/project_soul_20260624200409302.md`、`reportPath=novel/reviews/book_ac7722d0/outline/outline_manual_edit_20260624200409304.json`。
- 归档策略：大纲保存前写 `before_outline_edit` 快照；Project Soul 保存前写独立 `.md` 快照；`novel/reviews/{bookId}/outline` 写 `outline_manual_edit` 报告。
- 前端验证：`OutlineEditor` 新增“编辑大纲”入口，可编辑 Project Soul、编辑说明和完整大纲 JSON；保存后刷新当前大纲；`docker compose build frontend` 通过。
- 清理说明：烟测中用于验证返回体的极简 ASCII 大纲已从自动快照恢复回完整《玄灯旧案》大纲和原 Project Soul，避免污染后续章节生成测试数据。

新增大纲审查与边界补齐烟测细节：

- Python Agent 注册：`outline_review` 已注册，agent class 为 `OutlineReviewAgent`；当前 `GET http://localhost:8000/api/agents/list` 返回 15 个 Agent。
- Java 文档兼容入口：`POST /api/projects/20260624_164140_41ac/outline/review`。
- 审查任务：`task_b0badab7e17e4dce`。
- 任务状态：`SUCCESS`。
- 审查报告：`novel/reviews/book_ac7722d0/outline/outline_review_20260624204358548583.json`。
- 报告读取 API：`GET /api/projects/20260624_164140_41ac/books/book_ac7722d0/outline/reviews` 返回最新 `outline_review` 报告。
- 审查结果：`score=73`、`status=needs_revision`、`totalChapters=3`、`chaptersWithBoundary=0`、`missingBoundaryCount=3`。
- 主要发现：3 个章节均缺少 `core_goal`、`must_write`、`allowed_progress`、`must_not_write`、`reserved_for_future`、`stop_point`、`ending_hook`。
- 自动补齐任务：`task_84ac8f36e7d4463c`，请求 `auto_fix=true`，任务 `SUCCESS`。
- 自动补齐结果：补齐 3 章，每章写入 `core_goal`、`must_write`、`allowed_progress`、`must_not_write`、`reserved_for_future`、`stop_point`、`ending_hook`；修复后 `chaptersWithBoundary=3`、`missingBoundaryCount=0`。
- 修复归档：补齐前快照写入 `novel/outline/versions/book_ac7722d0/book_ac7722d0_outline_before_boundary_fix_20260624205626130428.json`；修复报告写入 `novel/reviews/book_ac7722d0/outline/outline_boundary_fix_20260624205626132103.json`。
- 覆盖重建任务：`task_854ecb76c48b4322`，请求 `auto_fix=true`、`overwrite_boundary=true`，任务 `SUCCESS`。
- 覆盖重建结果：评分 `100`，`status=passed`，`chaptersWithBoundary=3`，`missingBoundaryCount=0`，0 错误、0 警告；用于清理弱模型/旧规则生成的过泛边界描述。
- 前端验证：`GET http://localhost/projects/20260624_164140_41ac/outline` 返回 200；`OutlineEditor` 已有“审查大纲”“补齐边界”按钮和“审查报告”标签页。
- 限制说明：当前已能自动补齐和覆盖重建章节边界字段，但还没有形成正式审批流，也缺真实 LLM 大纲质量评分基准。

## 3. 与旧评估相比已修复/前进的点

旧报告中的若干 P0 阻塞已经解决：

| 原问题 | 当前状态 |
| --- | --- |
| Python f-string / Optional 导入导致无法编译 | 已通过 `compileall` |
| Java 调 Python Agent 名称不匹配 | `PythonClientService` 已做旧类名到注册 key 的映射 |
| Java 发送 `parameters`，Python 读 `config` 不一致 | `AgentRequest` 同步 `parameters` 和 `config`；Java 也同时发送两者 |
| Python 小写状态 Java enum 不识别 | `TaskExecutorService` 已做状态映射 |
| `output_refs/errors/warnings` 为 List，Java 期望 Map | Java 已包装为 JSON map |
| chunk 字段 `content`/`text` 不一致 | `FullTextAnalysisAgent` 已兼容两者 |
| 缺 `/api/projects/{projectId}/tasks/execute` | 已新增 `ProjectTaskController` |
| `sample_import` 找不到 Java 上传文件 | 已优先读取 Java 传入的 `file_path` |
| 样本导入结果不回写样本状态 | `TaskExecutorService` 已在成功后回写 metadata 和 `CHUNKED` |
| GraphBuilder 未注册 | 已注册为 `graph_build`；当前 Python Agent 列表共 12 个，已包含 `chapter_revision` 和 `outline_review` |
| Java Docker healthcheck 使用缺失的 curl | 已改为 `wget`，容器健康状态恢复 |
| 本地无法无 API 跑全文分析 | 已增加 `MOCK_LLM` 开关，mock 模式下全文分析/单书报告可跑通 |
| 文档分析路径与实际路径不一致 | 全文分析和单书报告已双写/优先使用 `analysis/per_chunk`、`analysis/per_book`，保留旧路径兼容 |
| 跨书归纳与 Skill 输入路径割裂 | `SkillGeneratorAgent` 已优先读取 `analysis/cross_book`，并保留旧根目录兼容 |
| Skill 只写全局目录 | 已改为优先写 `projects/{projectId}/skills/local`，同时镜像旧 `/workspace/skills/{projectId}` 兼容 |
| 缺 `skills/enabled.yaml` | `skill_generation` 已生成项目级启用配置，包含 type、path、enabled、priority、scope |
| 大纲/正文读不到项目级 Skill | `OutlineGeneratorAgent`、`ChapterWriterAgent` 已优先读取 `projects/{projectId}/skills/local` |
| 前端 Skill API 无后端 | 已新增 `SkillController` / `SkillService`：列表、内容、enabled 配置、编辑保存、启停、优先级/scope 配置和冲突检测可用 |
| 前端生产环境直连 `localhost:8080` 导致 CORS | 已将前端默认 API baseURL 改为 `/api`，走 nginx 同源代理；浏览器烟测通过 |
| 缺 Project Soul 产物 | `OutlineGeneratorAgent` 已生成 `novel/soul/project_soul.md`，并在后续大纲/正文 prompt 中作为不可违背设定注入 |
| 缺大纲审查 Agent | 已新增 `OutlineReviewAgent`，Java 文档兼容入口和前端审查入口已接入；审查报告写入 `novel/reviews/{bookId}/outline` |
| 大纲缺章节边界字段 | 已新增 `OutlineBoundaryCompleter`；`OutlineGeneratorAgent` 保存前兜底补齐，`OutlineReviewAgent` 支持 `auto_fix` 和 `overwrite_boundary`，会先归档快照再写回补齐后的大纲 |
| 章节草稿路径不规范 | `ChapterWriterAgent` 已写入 `novel/chapters/drafts/{bookId}/volume_{n}`，并兼容读取旧章节上下文路径 |
| 检索/ContextBuilder 完全缺失 | 已新增最小 `retrieval/context_builder.py`；`ChapterWriterAgent` 会生成上下文包并注入 prompt；Java/前端已能读取 context packs |
| 大纲/章节前端读取接口缺失 | 已新增 `BookController` / `BookArtifactService`，支持 books 列表、default 最新书籍解析、大纲、Project Soul、章节列表、章节详情读取 |
| ProjectDetail/OutlineEditor/ChapterWriter 停留在占位/硬编码 | 已改为读取真实项目、任务、Skill、books、outline、soul、chapter APIs；章节生成使用真实 bookId |
| 缺分析状态和报告读取 API | 已新增 `AnalysisController` / `AnalysisArtifactService`，支持项目分析状态、项目报告汇总、样本 manifest/chunk/逐块分析/单书报告、跨书报告读取 |
| 任务缺重试和日志接口 | 已新增 `POST /api/tasks/{taskId}/retry`、`GET /api/tasks/{taskId}/logs` 与 `POST /api/tasks/{taskId}/resume`，`TaskResponse` 已暴露 errors、warnings、checkpointRef |
| 记忆管理仍停留在空 Markdown/Agent 雏形 | 已新增 `MemoryController` / `MemoryService`；`MemoryExtractorAgent` 能写项目级 JSON、同步 6 类 Markdown 并生成章节快照；`MemoryView` 已接真实 API |
| 图谱只有注册 Agent、没有业务接口 | 已新增 `GraphController` / `GraphArtifactService`；`GraphBuilderAgent` 优先读取项目级记忆并写标准 `graph/graph.json`；`GraphView` 已接真实图谱 API |
| Python/Java/前端缺统一产物浏览入口 | 已新增 `ArtifactController` / `ArtifactService` 与 `ArtifactView`，支持按分类聚合、搜索列表、文本/JSON 预览、下载、批量导出、归档、恢复、归档永久删除、文本差异对比、审计日志、敏感样本授权保护和项目内路径安全校验 |

这意味着当前最优先目标已经从“修启动/协议”变成“补业务闭环、持久化、前端可用性和分析链路稳定性”。

## 4. Java 后端实现度

### 已实现

- `ProjectController`
  - `POST /api/projects`
  - `GET /api/projects`
  - `GET /api/projects/{projectId}`
  - `DELETE /api/projects/{projectId}`，目前为归档删除
- `SampleController`
  - `POST /api/projects/{projectId}/samples`
  - `GET /api/projects/{projectId}/samples`
  - `GET /api/projects/{projectId}/samples/{sampleId}`
  - `DELETE /api/projects/{projectId}/samples/{sampleId}`
  - `POST /api/projects/{projectId}/samples/{sampleId}/analyze`
  - `POST /api/projects/{projectId}/samples/analyze`
  - `GET /api/projects/{projectId}/samples/{sampleId}/structure`
  - `POST /api/projects/{projectId}/samples/{sampleId}/structure/sync`
- `AnalysisController`
  - `GET /api/projects/{projectId}/analysis/samples/{sampleId}/analysis/db`
  - `POST /api/projects/{projectId}/analysis/samples/{sampleId}/analysis/sync`
  - `GET /api/projects/{projectId}/analysis/samples/{sampleId}/chunks/{chunkId}/analysis/db`
- `TaskController`
  - `GET /api/tasks/{taskId}`
  - `GET /api/tasks?projectId=...`
  - `POST /api/tasks/{taskId}/cancel`
- `ProjectTaskController`
  - `POST /api/projects/{projectId}/tasks/execute`
- `SkillController`
  - `GET /api/projects/{projectId}/skills`
  - `GET /api/projects/{projectId}/skills/enabled`
  - `GET /api/projects/{projectId}/skills/conflicts`
  - `GET /api/projects/{projectId}/skills/{skillName}`
  - `PATCH /api/projects/{projectId}/skills/{skillName}`
  - `PATCH /api/projects/{projectId}/skills/{skillName}/config`
  - `POST /api/projects/{projectId}/skills/{skillName}/enable`
  - `POST /api/projects/{projectId}/skills/{skillName}/disable`
- `BookController`
  - `GET /api/projects/{projectId}/books`
  - `GET /api/projects/{projectId}/books/{bookId}/outline`
  - `GET /api/projects/{projectId}/books/{bookId}/soul`
  - `GET /api/projects/{projectId}/books/{bookId}/chapters`
  - `GET /api/projects/{projectId}/books/{bookId}/chapters/{volumeNumber}/{chapterNumber}`
  - `GET /api/projects/{projectId}/books/{bookId}/outline/reviews`
- `ChapterController` 文档兼容入口
  - `POST /api/projects/{projectId}/outline/review`
- 实体与 Repository
  - `Project`
  - `Sample`
  - `SampleChapter`
  - `SampleChunk`
  - `AnalysisResult`
  - `Task`
- 服务层
  - 项目目录创建
  - 样本上传、hash 去重、文件校验、读取、删除
  - 样本 manifest/chunk 文件同步到 `sample_chapters`、`sample_chunks`，并根据逐块分析产物标记 chunk `processed`
  - 逐块分析 JSON 同步到 `analysis_results`，结构化保存摘要、钩子、技法计数、完整 `analysis` JSONB 和 `raw_result` JSONB
  - 异步任务执行、状态映射、错误包装、重试、取消
  - Java -> Python Agent 调用与协议兼容
  - 项目级 Skill Markdown 与 `enabled.yaml` 读取、编辑、启停、优先级/scope 配置、版本列表、版本恢复、快照归档、编辑/恢复报告和基础冲突检测
  - Python 生成的大纲、Project Soul、章节草稿/终稿文件读取，并支持 `bookId=default` 解析最新书籍
  - 大纲审查任务创建、大纲审查报告读取与 `novel/reviews/{bookId}/outline` 归档
  - 章节草稿发布为终稿，并在发布后自动创建记忆摄取任务
  - 项目级 6 类记忆 Markdown、JSON 条目、章节快照、记忆任务状态读取，并将记忆聚合快照同步到 `memory_artifacts` 表（手工 `/memory/sync` 与 `memory_extraction` 任务后自动同步）
  - 项目级知识图谱读取、节点/子图/路径查询、重建任务触发、JSON/GraphML/Cytoscape/HTML 导出
  - 项目级检索索引摘要、上下文包列表与上下文包详情读取
- 数据库
  - 使用 PostgreSQL
  - `init.sql` 启用 pgvector
  - JPA 自动管理项目、样本、任务 3 个核心实体表，以及新增的 `sample_chapters`、`sample_chunks`、`analysis_results`、`model_profiles`、`skill_profiles`、`outline_artifacts`、`chapter_artifacts`、`memory_artifacts`、`graph_artifacts` 业务表

### 缺口

| 文档要求 | 当前缺口 |
| --- | --- |
| 完整数据库 schema | `sample_chapters`、`sample_chunks`、`analysis_results`、`model_profiles`、`skill_profiles`、`outline_artifacts`、`chapter_artifacts`、`memory_artifacts`、`graph_artifacts`、`retrieval_artifacts` 已补齐并可用；样本/分析/模型/Skill/大纲/章节/记忆/图谱/检索全部核心产物均已落 DB 业务表；基础迁移治理已收紧为 Flyway 迁移 + Hibernate `validate`，默认/prod/ci 配置均有测试守护；仍缺迁移回滚策略、线上变更评审和跨环境迁移演练 |
| 大纲数据库持久化 | 已新增 `OutlineArtifact` / `OutlineArtifactRepository` / `OutlineArtifactService`，`outline_artifacts` 保存大纲 JSONB、Project Soul 文本、最新审查报告 JSONB、总卷数/总章数/边界完整章数/审查分数/路径等摘要字段；新增 `POST /api/projects/{projectId}/outline/sync`、`GET /api/projects/{projectId}/outline/db`、`POST /api/projects/{projectId}/books/{bookId}/outline/sync`、`GET /api/projects/{projectId}/books/{bookId}/outline/db` 和 `GET /api/projects/{projectId}/books/outlines/db`；`outline_generation`、`outline_review` 成功和大纲手工保存后会自动同步；烟测项目 `20260624_164140_41ac` 已验证 `book_ac7722d0` 写入 DB，`total_chapters=3`、`chapters_with_boundary=3`、`review_status=passed`、`latest_review_score=100`、`outline` 为 JSONB object |
| 章节数据库持久化 | 已新增 `ChapterArtifact` / `ChapterArtifactRepository` / `ChapterArtifactService`，`chapter_artifacts` 按 `project_id/book_id/volume_number/chapter_number/stage` 唯一保存草稿和终稿；字段包含正文、章节完整 JSONB、边界检查 JSONB、返修历史、人工审查历史、手工编辑历史、最新审查报告、版本、状态、字数、质量分、边界错误/警告统计、路径等；新增 `POST /api/projects/{projectId}/books/{bookId}/chapters/sync`、`GET /api/projects/{projectId}/books/{bookId}/chapters/db`、单章 `/sync` 和单章 `/db`；章节写作、独立返修、终稿发布、版本恢复、人工审查和手工编辑后会自动同步；烟测项目已验证 4 条章节产物入库（3 草稿、1 终稿），第 3 章可读回 `version=6`、`boundary_passed=true`、`revision_history=1`、`manual_edit_history=2` |
| 模型配置服务 | `ModelProfileController` / `ModelProfileService` 已改为以 DB `model_profiles` 为权威存储，支持从旧 `config/model_profiles.json` 初始化、默认 mock profile、列表/创建/更新/删除/设默认/结构与连通性测试、API Key AES-GCM 加密入库、API 掩码返回和任务默认 `model_profile_id` 注入；Java 会导出 `/workspace/config/model_profiles.json` 作为 Python 运行时兼容文件，但其中只保留 `apiKeyRef` 和 `secretsFile`，实际 key 隔离到 `config/model_profile_secrets.json`；Python `LLMClient` 已能按请求级 `model_profile_id` 和任务类型选择 main/fast/embedding/rerank 模型、解析运行时 secret 引用，支持 fallback、缓存、限流、成本估算、用量台账并把实际模型写入任务 metrics；仍缺真实供应商 API Key 调用测试、生产级配额/成本治理和 secrets provider/轮换治理 |
| Skill 管理服务 | 已有 SkillController/SkillService 支持读取、编辑、启停、优先级/scope 配置、版本列表、版本差异对比、版本恢复、快照归档、编辑/恢复报告、基础冲突检测、冲突审计报告、规则型质量校验、语义质量详情和批准/驳回审批；`skill_profiles` 已以项目级聚合 profile 落库，包含内容快照、enabled 配置、task overrides、质量/审批元数据和最新冲突报告摘要，并在 `skill_generation` 成功、编辑、配置、质量校验、审批、恢复后自动同步；前端已能创建重新生成任务并预览最近一次生成结果；仍缺真实模型语义评审和更深层冲突自动合并 |
| 记忆数据库持久化 | 已新增 `MemoryArtifact` / `MemoryArtifactRepository` / `MemoryArtifactService`，`memory_artifacts` 按 `project_id` 唯一保存 6 类记忆 Markdown 快照、记忆 JSON（人物/世界观/剧情/伏笔/时间线）、最新章节快照、快照列表、最近记忆任务和摘要计数字段（characterCount/worldSettingCount/plotCount/suspenseCount/timelineEventCount/markdownCount/snapshotCount）；新增 `GET /api/projects/{projectId}/memory/db`、`POST /api/projects/{projectId}/memory/sync`；`memory_extraction` 任务成功后会在 `TaskExecutorService` 自动同步，前端 `MemoryView` 已有“同步记忆数据库”按钮；连续性/审计报告已支持人工处理和确定性修复事件归档，连续性缺失出场记录可自动 append，人物/设定首次提及章节可自动 set，时间线可按章节排序修复；仍缺深度冲突合并和工作流级人工确认 |
| 图谱数据库持久化 | 已新增 `GraphArtifact` / `GraphArtifactRepository` / `GraphArtifactDbService`，`graph_artifacts` 按 `project_id+book_id` 唯一保存图谱 JSONB、节点/边列表、节点/边类型分布、平均度/密度、Top 度节点、Top 中心度节点、桥接节点、关系类型分析、关键路径、增量构建摘要、人物/地点/组织/物品计数、图谱任务和元数据；新增 `GET /api/projects/{projectId}/graph/db`、`POST /api/projects/{projectId}/graph/sync`、book 级 `/books/{bookId}/graph/db`、`/books/{bookId}/graph/sync` 和 `/graphs/db` 列表；`graph_build` 任务成功后会在 `TaskExecutorService` 自动同步；图谱版本审计已支持 `/graph/versions` 手动归档、列表/详情读取和重建前自动归档，前端 `GraphView` 已有“同步图谱数据库”按钮、高级分析摘要和版本抽屉；伏笔节点已补 involvement / resolved_by 基础关系；仍缺从章节正文/大纲/样本技巧中抽取更多实体关系 |
| 大纲/章节/记忆/图谱 API | 大纲、Project Soul、章节草稿/终稿、章节返修任务、章节版本快照/恢复、章节人工审查、章节手工编辑保存、大纲/Project Soul 编辑保存、大纲审查、审查归档、记忆总览/分类/快照、记忆 DB 同步/读取、图谱读取/查询/重建/导出、图谱 DB 同步/读取 API 已有；终稿多版本状态机已补齐；仍缺工作流级审批等业务端点 |
| 检索上下文 API | 已有 context pack 只读 API；Python 侧已能在章节上下文构建和独立重建任务中写入 `retrieval_plan`、hybrid 检索结果、hash-vector 摘要、hybrid 摘要、质量评估、引用预算和缓存状态；Java 已暴露检索配置、BM25/vector/hybrid 摘要、重建任务、context pack 详情、质量评估报告、benchmark 报告、缓存失效、索引版本和 benchmark 运行 API；新增 `retrieval_artifacts` DB 业务表与 `GET /retrieval/db`、`POST /retrieval/sync`、`GET /retrieval/list/db`，保存检索配置、三类索引摘要、重建报告、质量报告、benchmark 报告、context pack 列表、文档计数、检索任务和 benchmark 摘要指标，并在 `retrieval_index` 任务成功后自动同步，前端检索页已加“同步检索数据库”、版本抽屉和 benchmark 展示；前端已有检索管理页。仍缺正式向量检索服务、真实 embedding、外部 rerank |
| 任务编排 | 已新增文件型 `workflow` Agent、每节点 checkpoint、人工确认暂停、并行节点、条件节点、Java `/resume` 恢复入口和前端 TaskCenter/ProjectDetail 审批 UI；仍缺 LangGraph 持久状态、更完整阶段状态机和更多正式工作流模板 |
| 结果产物 API | 已有统一 artifact 查询/分类列表/文本与 JSON 预览/下载接口，并接入前端 ArtifactView；归档/恢复/归档永久删除、批量 ZIP 导出、文本差异对比、审计日志查询、敏感样本授权保护、操作级角色校验、拒绝审计和 30 天归档保留策略已实现，并补 `ArtifactServiceTest` 定向覆盖；ArtifactController 已优先使用请求头身份上下文，避免 body/query 伪造角色；仍缺正式用户/组织权限系统和权限审计 UI |
| 权限与安全 | artifact 路径已限制在项目根目录内，路径穿越烟测返回 400；Artifact API 已对 `samples/raw`、`samples/normalized`、`samples/chunks` 执行敏感样本预览/下载/diff/批量导出授权保护；模型 API Key 已加密入库、API 响应脱敏，且运行时 `model_profiles.json` 已通过 `apiKeyRef` 与 `model_profile_secrets.json` 做配置/secrets 文件隔离；Java 已新增请求头身份上下文、可强制项目访问校验、mutation 角色门槛、prod 默认强制认证、403 处理和 Java->Python 身份 header 转发；仍缺 JWT/OAuth、用户/组织/成员数据模型、细粒度 RBAC 策略、生产级 secrets provider/轮换/审计 |

Java 后端当前适合作为 MVP 网关，但还没有成为完整业务中台。

## 5. Python AI 服务实现度

### 已实现

- FastAPI 服务：
  - `/health`
  - `/api/agents/list`
  - `/api/agents/{agent_name}/run`
  - `/api/agents/tasks/{task_id}/cancel`，目前只是占位返回
- 已注册 15 个 Agent：
  - `sample_import`
  - `full_text_analysis`
  - `book_summary`
  - `cross_book_synthesis`
  - `skill_generation`
  - `outline_generation`
  - `outline_review`
  - `chapter_writing`
  - `chapter_revision`
  - `memory_extraction`
  - `memory_query`
  - `graph_build`
  - `coverage_check`
  - `retrieval_index`
  - `workflow`
- 基础组件：
  - `AgentRequest` / `AgentResponse`
  - `BaseAgent`
  - LiteLLM 客户端
  - PromptBuilder
  - TextNormalizer
  - ChapterDetector
  - Chunker
  - CoverageValidator
  - SkillLoader 与 3 类 skill 模板
- `sample_import` 已实际跑通：
  - 读取 Java 上传文件
  - 规范化文本
  - 检测章节
  - 分块
  - 覆盖率校验
  - 保存 normalized/chunks/manifest
  - 返回结构化 metadata
- `memory_extraction` 已实际跑通：
  - 优先读取 `novel/chapters/final/{bookId}`，并兼容草稿与旧路径的章节 JSON
  - 兼容 camelCase/snake_case 章节字段
  - 写回 book 级记忆 JSON 与项目级 `memory/*.json`
  - 同步 6 类记忆 Markdown
  - 生成 `memory/snapshots/{bookId}_v{volume}_c{chapter}.json`
- `graph_build` 已实际跑通：
  - 优先读取项目级 `memory/*.json`，兼容旧 book 级 memories 路径
  - 从人物、世界观、剧情、伏笔、时间线记忆构建节点和关系
  - 规范化中文设定类型，如“组织”映射为 `organization`
  - 写入标准 `graph/graph.json`、按书籍命名图谱和 book 级兼容图谱文件
- `ContextBuilder` 已接入章节生成：
  - 汇入样本 chunks、单书报告、跨书报告、项目 Skill、记忆 Markdown、图谱节点和前文摘要
  - 生成 `indexes/bm25/index_summary.json`
  - 生成 `indexes/bm25/context_packs/{bookId}_v{volume}_c{chapter}.json`
  - 将上下文包 prompt section 注入 `ChapterWriterAgent`
- 本地混合检索雏形已接入 ContextBuilder：
  - `retrieval/hybrid_engine.py` 提供 `RetrievalPlan`、`HashVectorIndex` 和 `HybridRetrievalEngine`
  - 章节上下文构建时执行关键词检索、hash-vector 检索、图谱检索、RRF 融合和规则 rerank
  - 上下文包中写入 `retrieval_plan`、`hybrid_retrieval`、`keyword_results`、`retrieval_results`
  - 额外落盘 `indexes/vector/index_summary.json` 与 `indexes/hybrid/index_summary.json`
- `BoundaryChecker` 已接入章节生成：
  - 章节 schema 已补 `core_goal`、`must_write`、`allowed_progress`、`must_not_write`、`reserved_for_future`、`stop_point`、`ending_hook`。
  - 旧大纲缺边界字段时，会根据当前章纲和后续章纲生成最小边界控制与后续保护清单。
  - 生成后写入 `boundary_check`，并将 blocking errors / warnings 回传任务结果。
- `OutlineReviewAgent` 已接入大纲审查：
  - 支持 `outline_review` 任务。
  - 读取 `novel/outline/{bookId}_outline.json` 与 `novel/soul/project_soul.md`。
  - 检查大纲必填字段、章节必填字段、章节边界字段、重复章节号和明显边界冲突。
  - 生成 `novel/reviews/{bookId}/outline/outline_review_*.json`，并返回 score、status、metrics 和 findings。
- `OutlineBoundaryCompleter` 已接入大纲生成和审查：
  - `OutlineGeneratorAgent` 保存前兜底补齐章节边界字段。
  - `OutlineReviewAgent` 支持 `auto_fix` 自动补齐缺失字段。
  - 支持 `overwrite_boundary` 覆盖重建已有边界字段，修复过泛或弱约束。
  - 自动补齐前会写大纲快照，并写入 `outline_boundary_fix` 报告。

### 缺口

| 模块 | 当前状态 | 主要缺口 |
| --- | --- | --- |
| LLM 客户端 / Model Gateway | LiteLLM 简单封装 + mock 模式已存在；Java 已补全 DB-backed 模型 profile 管理、默认配置、结构/连通性测试、API Key 加密入库、接口脱敏和任务 `model_profile_id` 注入；Python `LLMClient` 通过 Java 导出的运行时兼容 `config/model_profiles.json` 读取配置，支持解析 `apiKeyRef` 到独立的 `config/model_profile_secrets.json`，并按请求级 profile/任务类型选择模型；profile fallback、响应缓存、`min_interval_ms`/RPM 限流、token/成本估算、usage JSONL 台账、请求级 usage summary、embedding/rerank 本地降级和任务 metrics 记录已有定向测试覆盖 | 缺真实供应商 API Key 调用测试、生产级配额/成本看板、集中 secrets provider、轮换和权限审计 |
| FullTextAnalysisAgent / CoverageCheckAgent | mock 模式已稳定跑通；全文分析会自动写覆盖率报告，独立 `coverage_check` Agent 可重算文本分块覆盖率和逐块分析覆盖率；全文分析已支持逐 chunk checkpoint、复用已有 chunk 分析结果、Java checkpointRef 回写和 `/resume` 后跳过已完成 chunk；覆盖率检查可生成失败/缺失 chunk 修复队列，`auto_repair` 可调用 `analysis_repair` 定向补偿，Java 会在覆盖率任务后同步 DB 诊断状态；任务层已支持 `retry_policy`/`max_retries` 配置和指数退避诊断记录 | 真实 LLM 稳定性、失败详情精细 UI 和更深分析质量校验仍需加强 |
| BookSummary / CrossBook | 单书与双样本跨书 mock 链路已跑通 | 真实 LLM 报告深度、质量校验、更多样本规模验证仍需加强 |
| SkillGenerator | 已生成项目 `skills/local` 与 `enabled.yaml`，并保留旧目录兼容；Java/前端已支持生成/重新生成任务入口、生成状态反馈、生成结果预览、编辑、启停、配置、版本差异对比、版本恢复、基础冲突检测、冲突审计报告、规则型质量校验、语义质量详情和审批状态写入；Java 已把项目 Skill 聚合到 DB `skill_profiles`，并保存内容快照、task overrides 和最新冲突报告摘要 | 缺真实模型语义评审和更深层冲突自动合并 |
| OutlineGenerator / OutlineReviewAgent | 有 schema 和 prompt，保存优先写 `novel/outline`，加载项目级 Skill，并生成 `project_soul.md`；章节 schema 已补边界字段；规则型 `OutlineReviewAgent` 已能审查字段完整性、章节边界和冲突，并归档报告；`OutlineBoundaryCompleter` 已能在生成后和审查时补齐/重建边界字段；Java/前端已支持大纲与 Project Soul 手工编辑保存、快照、报告归档、审查报告查看和边界补齐入口 | 缺真实 LLM 大纲质量评估、更细粒度结构化编辑、人工审批流和边界补齐后的人工确认 |
| ChapterWriter / RevisionAgent | ChapterWriter 有生成与审查 prompt，加载项目级 Skill、Project Soul、检索上下文包和边界控制；RevisionAgent 可独立读取草稿、按人工/边界问题返修、写 `revision_history`、归档版本快照和 `novel/reviews` 报告；Java/前端已支持历史版本恢复并在恢复前归档当前版本；章节人工审查可写入 `approved/needs_revision/rejected` 决策和 `chapter_human_review` 报告；章节手工编辑会归档当前版本、递增版本并写 `chapter_manual_edit` 报告；草稿保存、终稿发布、终稿覆盖前快照、终稿版本递增、恢复为终稿、章节差异对比、批量发布和 `chapter_finalize` 审计已对齐；`chapter_pipeline` 工作流已有人工确认节点并可从 ProjectDetail/TaskCenter 审批恢复 | 缺真实 LLM 下的返修质量验证 |
| MemoryExtractor/Query | 项目级 JSON、6 类 Markdown 同步、快照、查询入口和终稿后自动触发已跑通；Java 已新增 `memory_artifacts` DB 业务表并在 `memory_extraction` 成功后自动同步；连续性/审计检查可落报告并对人物出场缺失、人物首次提及、设定首次提及和时间线排序倒挂生成/应用确定性修复，修复前有版本快照，修复后有事件归档 | 缺更广泛连续性场景、深度冲突合并、按人物/伏笔/认知的深度合并策略 |
| GraphBuilder | 已能从项目级记忆、大纲、章节正文和跨书样本技巧汇总生成图谱，并通过 Java API/前端查询展示；图谱节点已被 ContextBuilder 纳入章节上下文；Python 已产出中心度、桥接、关系类型分析、关键路径和增量构建摘要；人物、设定、剧情、时间线、伏笔、章节大纲、章节正文和技巧已能生成基础关系，伏笔可连到相关人物/设定与解决它的剧情节点，章节大纲可连到涉及人物/设定/伏笔/剧情目标和使用技巧，章节正文可连到对应大纲、被正文提及的实体和使用技巧；Java 已新增 `graph_artifacts` DB 业务表并在 `graph_build` 成功后自动同步高级分析字段；图谱版本快照可手动创建、列表/详情读取，重建前自动归档当前图谱 | 缺真实 LLM 关系抽取和更强交互式图谱 |
| retrieval | 已有关键词/BM25 风格检索、ContextBuilder、本地混合检索、独立 `retrieval_index` 重建 Agent、Java 配置/索引摘要/重建/质量评估/缓存失效/API/benchmark 和前端 RetrievalView；上下文包能落盘、被 Java API 读取并注入章节 prompt；Python 侧已有 hash-vector、RRF 融合、规则 rerank、Retrieval Plan、质量评估、引用预算、缓存状态、索引版本、离线 benchmark 和 vector/hybrid 摘要产物；通用 Artifact API 可预览/下载索引产物，DB 聚合层已同步质量报告与 benchmark 报告 | 缺 LanceDB/pgvector 正式向量库、embedding 模型实调和外部 reranker |
| orchestrator/checkpoint | 已新增 `orchestrator/workflow_engine.py`、`WorkflowAgent`、内置 `sample_analysis` / `retrieval_refresh` / `human_review_gate` / `chapter_pipeline` 等工作流、每节点 checkpoint、人工确认暂停、并行节点、条件节点、Java `/resume` 恢复入口和前端审批 UI；已通过人工确认暂停后恢复继续执行的烟测 | 缺 LangGraph 持久状态、阶段状态机和更完整的工作流模板 |

Python 当前的强项是样本导入、Agent 外壳和章节局部闭环，弱项是“分析之后如何沉淀并被大纲/正文稳定、可追踪、可恢复地使用”。

## 6. 前端实现度

### 已实现

- Vite + React + TypeScript + Ant Design 可 Docker 构建。
- 页面结构已覆盖主要功能：
  - Dashboard
  - ProjectList
  - ProjectDetail
  - SampleManagement
  - OutlineEditor
  - ChapterWriter
  - MemoryView
  - GraphView
  - RetrievalView
  - ArtifactView
- `ProjectList` 已接真实项目 API，支持创建和归档删除。
- `SampleManagement` 已接样本列表、上传、删除和分析任务入口。
- `SampleManagement` 已接分析产物 API：
  - 项目分析状态与进度统计。
  - 运行中任务自动轮询。
  - 样本 manifest、分块列表、逐块分析摘要、单书报告查看。
  - 跨书归纳触发与跨书报告查看。
  - 最近任务、任务详情和失败任务重试入口。
- `api.ts` 已封装 project/sample/task/skills/outline/chapter/memory/graph/analysis/retrieval/artifact 调用。
- `api.ts` 默认使用同源 `/api`，生产环境通过 nginx 代理到 Java 后端，避免浏览器跨域。
- `ProjectDetail` 已改为真实数据面板：
  - 读取项目详情、样本、任务、Skill、书籍和 Project Soul。
  - 展示工作流进度、统计、最近任务、Skill 列表、内容抽屉、编辑弹窗、启停操作、历史版本恢复、冲突提示、大纲/Project Soul 摘要、章节入口。
- `OutlineEditor` 已接入真实 books/outline/soul API：
  - 支持书籍选择。
  - 支持查看最新大纲和 Project Soul。
  - 支持触发大纲审查任务，轮询任务状态，并展示最新审查报告、评分、指标和 findings。
  - 支持触发边界补齐/覆盖重建任务，并在审查报告中展示自动补齐结果。
  - 生成新大纲时继续走任务 API。
- `ChapterWriter` 已接入真实 books/chapters/chapter content/finalize/boundary API：
  - 使用最新或用户选择的真实 bookId。
  - 能查看章节列表和章节内容。
  - 能读取并展示匹配章节的检索上下文包摘要。
  - 能展示章节边界检查通过/警告/阻塞结果。
  - 能将草稿发布为终稿，并触发记忆摄取任务。
  - 无大纲时显示空状态，避免继续传 `book_id=default` 给 Python 生成任务。
- `MemoryView` 已接入真实 memory API：
  - 显示 6 类记忆总览、条目数、更新时间和文件路径。
  - 支持查看 Markdown 内容与 JSON 条目。
  - 支持查看快照列表、最近记忆任务和手动重建入口。
  - 新增“同步记忆数据库”入口，可触发 `POST /memory/sync` 将记忆聚合快照写入 `memory_artifacts`。
- `GraphView` 已接入真实 graph API：
  - 支持书籍选择、图谱构建任务、轮询构建状态。
  - 显示节点/关系统计、节点表、关系列表和轻量 SVG 网络。
  - 支持 JSON、GraphML、HTML 导出入口。
  - 新增“同步图谱数据库”按钮，可把当前图谱聚合写入 `graph_artifacts` 表（`graphApi.getDb/syncDb`）。
  - 新增图谱版本按钮和版本抽屉，可手动创建版本快照、查看版本列表和详情；后端重建图谱前会自动归档当前图谱。
- `RetrievalView` 已接入真实 retrieval API：
  - 支持查看检索配置、BM25/vector/hybrid 索引摘要、上下文包列表和上下文包详情。
  - 支持保存检索配置并触发索引重建任务。
- `ArtifactView` 已接入真实 artifact API：
  - 支持 8 类项目产物聚合统计、分类筛选、路径搜索、内容预览和下载。
  - JSON 产物可返回结构化 JSON；下载与预览均做项目内路径校验。

### 缺口

| 页面/功能 | 当前问题 |
| --- | --- |
| Dashboard | 已接 `/api/dashboard` 真实聚合数据，展示项目/样本/章节/任务统计、服务健康、最近项目/最近任务、工作流进度、阻塞项目、下一步动作、任务性能窗口和基础告警中心；告警已支持确认、静默、重开、状态摘要和条件变化后重新激活；已新增基础长期趋势快照表、趋势 API、前端趋势折线图、采样间隔/保留天数策略、告警通知 outbox、webhook/email 渠道配置状态、critical/warning 基础升级判定、真实 webhook/email 投递、投递回执和失败重试、通知订阅人/分组路由/模板治理、投递历史筛选，以及只读运维看板；TaskCenter 已有 SSE 实时日志推送；仍缺生产级连接治理和更完整权限审计 |
| ProjectList | 创建请求已传递 `genre`/`sampleGroupType`，后端已保存并回显题材，状态映射已覆盖当前项目状态枚举；前端已支持搜索、状态/样本分组筛选、列排序、批量归档、状态下一步引导和 Dashboard 创建入口；剩余主要是更细项目状态与真实工作流状态联动 |
| SampleManagement | 已映射后端 `title/fileName/createdAt/status/totalChars/totalChapters`，并接入分析状态、任务轮询、详情抽屉、manifest/chunk/报告查看、覆盖率校验/展示、失败/缺失 chunk 聚合诊断、问题块详情直达、单块定向修复，以及单书/跨书报告、覆盖率、问题块、Manifest、分块索引和分析包导出 |
| 样本上传后的分析流程 | 已避免上传后重复触发 `sample_import`；当前由用户点击“分析”触发全文分析 |
| Outline/Chapter/Memory/Graph | Outline/Chapter 已接真实 books/outline/soul/chapter/retrieval/finalize/boundary/revision/versions/reviews/restore/update/batch-finalize API，并支持 book 选择；OutlineEditor 已支持大纲/Project Soul 编辑保存、锁定/解锁/批准、大纲与 Project Soul 历史版本预览/恢复、大纲审查报告查看和边界补齐入口；ChapterWriter 已支持章节正文手工编辑保存、终稿发布、终稿多版本归档、恢复为草稿/终稿和批量终稿发布；Memory/Graph 已接真实 API；仍缺工作流级审批和更强交互式图谱 |
| Retrieval/Artifact | RetrievalView 已能管理配置、索引摘要、上下文包和重建任务；ArtifactView 已能分类查看、搜索、预览、下载、批量导出、归档/恢复/归档删除、差异对比、审计日志、敏感样本授权保护、请求头身份优先的治理角色传递和归档保留策略；仍缺正式用户/组织权限系统与权限审计 UI |
| Skill 页面/API | Java Skill 管理 API 已可用；ProjectDetail 已能展示 Skill 列表、查看内容、来源/证据、编辑 Markdown、调整启停/优先级/type/scope、触发规则型质量校验、查看语义质量详情、预览/下载质量报告、批准/驳回并预览审批报告、对比/恢复历史版本、恢复 enabled.yaml 配置版本，并展示冲突检测结果、生成/预览/下载冲突审计报告、重新生成入口、最近生成任务状态和生成结果预览；剩余重点是语义评审自动化与冲突自动合并 |
| 任务监控 | 样本页已有运行中任务轮询、最近任务、失败任务重试、日志详情和导入任务阶段化百分比；TaskCenter 已支持全局任务列表、项目/状态筛选、失败诊断、结构化事件日志、原始 JSONL 文件日志 Tail、SSE 实时日志推送、retry/resume/cancel、人工确认审批和 retry policy 退避展示；Dashboard 已补充最近任务性能窗口、慢任务/高重试/LLM 调用/token 聚合、基础告警中心、告警确认/静默状态、基础趋势快照、趋势折线图、采样/保留策略、通知 outbox、基础升级判定、真实 webhook/email 投递、投递回执、失败重试、订阅人/分组策略、模板治理、投递历史筛选和运维看板；仍缺生产级连接治理和更完整权限审计 |

前端已经从静态原型推进到“部分链路可演示”：项目详情、大纲、Project Soul、大纲审查、边界补齐、Skill 管理、章节页、样本分析页、记忆页、图谱页、检索页、产物页、任务中心、章节检索上下文、边界检查、章节手工编辑保存和终稿发布可以操作真实后端产物；距离完整业务可用还需要更完整的阶段状态机、更强交互式图谱和产物治理。

## 7. 工作区与文件规范实现度

### 已实现

项目创建时会生成较完整目录：

- `samples/raw`
- `samples/normalized`
- `samples/chunks`
- `samples/manifests`
- `analysis/per_chunk`
- `analysis/per_book`
- `analysis/cross_book`
- `analysis/coverage`
- `skills/local`
- `novel/soul`
- `novel/outline`
- `novel/chapters/drafts`
- `novel/chapters/final`
- `novel/reviews`
- `memory/snapshots`
- `graph`
- `indexes/bm25`
- `indexes/vector`
- `logs`
- `checkpoints`

并初始化 6 个记忆 Markdown：

- `characters.md`
- `foreshadowing.md`
- `timeline.md`
- `relationships.md`
- `cognition.md`
- `canon.md`

### 缺口

| 文档规范 | 当前差异 |
| --- | --- |
| Project Soul | 已由 `outline_generation` 生成 `novel/soul/project_soul.md`，并注入章节创作；前端已可查看和编辑；编辑时会保存 `.md` 快照；Java/前端已支持锁定、解锁、批准并锁定、版本列表、版本内容预览和恢复。剩余重点是把 Project Soul 变更接入更正式的工作流级审批 |
| Skill 启用配置 | 已由 `skill_generation` 生成 `skills/enabled.yaml`；Java/前端已支持重新生成入口、生成结果预览、启停、优先级/type/scope 编辑、基础冲突检测、冲突审计报告、内容版本对比/恢复、配置版本读取/恢复、配置快照、质量校验、语义质量详情、审批状态和报告归档；剩余重点是语义评审自动化与冲突自动合并 |
| 分析目录 | Python 已优先写 `analysis/per_chunk/{sampleId}`，并保留旧 `samples/analysis` 兼容；全文分析和 `coverage_check` 会写 `analysis/coverage/{sampleId}_coverage.json` |
| 单书/跨书报告目录 | 单书已写 `analysis/per_book`；跨书已写 `analysis/cross_book` 并保留旧根目录兼容 |
| 章节草稿/终稿目录 | ChapterWriter 已写入 `novel/chapters/drafts`；Java 发布接口已写入 `novel/chapters/final` 并保留源草稿引用；章节返修会覆盖当前草稿、递增 `version`，并在 `novel/chapters/versions` 归档返修前快照；历史版本恢复会先归档当前版本，再把选中快照恢复为新草稿/终稿；章节手工编辑会归档编辑前快照并写回当前章节；终稿覆盖发布会先归档旧终稿、递增终稿版本并写 `chapter_finalize` 报告；批量终稿发布会写 `chapter_batch_finalize` 汇总报告；章节对比接口可对比草稿、终稿和历史版本 |
| 章节边界产物 | Python 已在章节 JSON 写入 `boundary_check`，包含边界控制、后续保护清单、阻塞错误、警告和提示；自动返修会写入 `revision_history`；独立 RevisionAgent 已写入 `novel/reviews` 返修报告；版本恢复已写入 `chapter_restore` 报告；人工审查已写入 `human_review_history` 和 `chapter_human_review` 报告；手工编辑已写入 `manual_edit_history` 和 `chapter_manual_edit` 报告 |
| 记忆文件 | Java 初始化 6 类 Markdown；Python 记忆 Agent 已同步项目级 JSON、6 类 Markdown 与章节快照；终稿发布后自动摄取已跑通；记忆聚合快照已落库 `memory_artifacts` 表并提供 `/memory/db` 查询与 `/memory/sync` 同步；连续性/审计报告已能对人物出场记录缺失、人物提前出场、设定提前揭示和时间线排序倒挂应用带版本快照和事件归档的自动修复；仍缺深度冲突合并和工作流级人工确认 |
| 图谱文件 | Python 已写入 `graph/graph.json`、`graph/{bookId}_graph.json` 和 book 级兼容路径，且产出中心度、桥接、关系分析、关键路径和增量构建摘要；人物、设定、剧情、时间线、伏笔、章节大纲、章节正文和样本技巧基础关系已入图；Java 已能查询、导出、缓存查询和版本审计，并通过 `graph_artifacts` DB 业务表把图谱聚合（节点/边/统计/高级分析/增量摘要/任务/元数据）落库，`graph_build` 成功后自动同步；图谱版本写入 `graph/versions/{bookId}`，支持手动归档和重建前自动归档；仍缺真实 LLM 关系抽取和更强交互式图谱 |
| 索引目录 | `indexes/bm25` 已能写入索引摘要和章节上下文包；`indexes/vector` 已能写入本地 hash-vector 摘要，`indexes/hybrid` 已能写入最近一次混合检索计划、统计、top results、质量评估、引用预算、缓存状态和 benchmark 摘要；`indexes/retrieval_config.json`、`indexes/retrieval_index_report.json`、`indexes/retrieval_quality_report.json` 与 `indexes/retrieval_benchmark_report.json` 已接入 Java/前端；统一 Artifact API 已能预览和下载索引文件；新增 `retrieval_artifacts` DB 业务表，将检索配置、三类索引摘要、重建报告、质量报告、benchmark 报告、上下文包列表、检索任务、文档计数和 benchmark 摘要指标同步入库，支持 `GET /retrieval/db`、`POST /retrieval/sync` 和 `retrieval_index` 成功后自动同步；索引版本写入 `indexes/versions`，支持手动归档、列表/详情和重建前自动归档；仍缺正式向量库索引和真实 embedding/rerank |

目录骨架接近文档要求，主要产物已经有统一读回入口；剩余问题集中在更细的产物治理策略、生产级向量检索、真实 embedding/rerank 和外部 reranker。

## 8. 分阶段路线图完成度

| 阶段 | 文档目标 | 当前完成度 | 判断 |
| --- | --- | ---: | --- |
| Phase 1 基础框架 | Java/Python/DB/工作区/样本导入/章节分块 | 88% | 最小导入闭环和容器健康检查已跑通；样本导入已写入阶段化 `sample_import` checkpoint，任务列表可读取 latest checkpoint 显示真实百分比；`sample_chapters`、`sample_chunks` 已可从 manifest/chunk 文件同步入库；`model_profiles` 已 DB 化并验证 API Key 加密，运行时配置已通过 `apiKeyRef` 与独立 secrets 文件隔离；`skill_profiles` 已保存项目级 Skill 聚合快照；`outline_artifacts` 已保存大纲、Project Soul、最新审查报告和边界统计；`chapter_artifacts` 已保存草稿/终稿正文、章节 JSONB、边界检查和历史记录；`memory_artifacts` 已保存 6 类记忆 Markdown、记忆 JSON、快照列表和统计字段并在 `memory_extraction` 后自动同步；`graph_artifacts` 与 `retrieval_artifacts` 已覆盖图谱/检索聚合产物；数据库 schema 已由 Flyway 迁移管理且默认/prod/ci 均使用 Hibernate `validate`；仍缺迁移回滚/线上变更演练和生产级 secrets provider/轮换/权限管理 |
| Phase 2 样本拆解 | 全文分析、单书、跨书、覆盖率、索引、检索 | 89% | mock 全文分析、单书报告、双样本跨书归纳已跑通，且后端/前端可查看 manifest、分块、逐块分析、单书、跨书报告、覆盖率报告和失败/缺失 chunk 聚合诊断；样本章节/分块与逐块分析结果已入库；`coverage_check` 可重算文本分块覆盖率和逐块分析覆盖率，生成失败/缺失 chunk 修复队列，并通过 `auto_repair` 调用 `analysis_repair` 定向补偿；覆盖率任务完成后会同步 `sample_chunks.processed` 与 `analysis_results.status`，错误分析文件不再误标成功；`full_text_analysis` 已支持逐块 checkpoint 和 resume 跳过已完成 chunk，并在 Java 任务成功后自动同步 DB；任务层已支持可配置 `retry_policy`/`max_retries` 与指数退避诊断；关键词检索、本地 hybrid 检索、质量评估、引用预算、缓存失效、索引重建任务、检索配置 API/UI 已接入；新增 `retrieval_artifacts` DB 业务表，保存检索配置、BM25/vector/hybrid 索引摘要、重建报告、质量报告、benchmark 报告、上下文包列表、文档计数和 benchmark 摘要指标，并在 `retrieval_index` 成功后自动同步、支持 `/retrieval/db` 查询和 `/retrieval/sync` 手工同步；检索索引版本已支持手动快照、列表/详情和重建前自动归档；离线检索 benchmark 已支持固定查询集、命中率、MRR 和逐用例明细；真实 LLM、正式向量库、真实 embedding 和外部 rerank 未完成 |
| Phase 3 Skills 生成 | Skill 生成、解析、加载、冲突检测 | 80% | 项目级生成/重新生成入口、生成结果预览、启用配置、后端读取/编辑/启停/配置/版本差异对比/版本恢复/基础冲突检测、冲突审计报告、规则型质量校验、语义质量详情、审批状态、前端管理、生成状态反馈、质量/审批/冲突报告预览下载和快照报告已跑通；`skill_profiles` DB 聚合快照已可同步和查询，并保存最新冲突报告摘要；缺真实模型语义评审和更深层冲突自动合并 |
| Phase 4 大纲生成 | Project Soul、总纲/卷纲/章纲、边界控制、审查 | 70% | 大纲 Agent、路径、Skill 加载、Project Soul、读写 API、前端编辑、快照归档、历史版本预览/恢复、锁定/批准治理、边界字段 schema、规则型 OutlineReviewAgent 和 OutlineBoundaryCompleter 已对齐；当前已能审查、自动补齐和覆盖重建章节边界字段；大纲批准已增加边界字段完整性门槛和 override 审计；`outline_artifacts` 已将大纲 JSONB、Project Soul、最新审查报告、边界统计和摘要字段落库，并支持手工/自动同步；后续重点是逐章审批流、结构化编辑和真实 LLM 质量评估 |
| Phase 5 正文创作 | 章节生成、上下文、审查、返修、草稿/终稿 | 85% | 章节生成雏形、项目级 Skill、Project Soul、检索上下文包、BoundaryChecker、后续章纲保护、内嵌自动返修、独立 RevisionAgent、版本快照、历史版本恢复、章节人工审查确认、章节手工编辑保存、`novel/reviews` 归档、草稿/终稿目录、发布接口、终稿多版本状态机、读取 API、草稿/终稿/历史版本差异对比和批量发布策略已对齐；`chapter_artifacts` 已将草稿/终稿正文、章节 JSONB、边界检查、返修/审查/编辑历史和最新审查报告落库，并支持手工/自动同步；`chapter_pipeline` 工作流已有人工确认节点和前端审批恢复入口；缺真实 LLM 下返修质量验证 |
| Phase 6 记忆系统 | 6 类记忆、快照、一致性检查 | 55% | 项目级 6 类 Markdown 同步、JSON 条目、章节快照、Java API、前端 MemoryView 和终稿自动摄取已跑通；新增 `memory_artifacts` DB 业务表，保存 6 类 Markdown、记忆 JSON、快照列表、最新快照、记忆任务和统计元数据，并在 `memory_extraction` 成功后自动同步、提供 `/memory/db` 与 `/memory/sync` 接口和前端同步入口；连续性/审计报告已有人工处理，连续性缺失出场记录、人物首次提及、设定首次提及和时间线排序倒挂可自动修复并归档版本/事件；缺更广泛连续性规则、深度冲突解决和人工确认工作流 |
| Phase 7 知识图谱 | 图谱构建、查询、可视化、导出 | 62% | 项目级图谱构建、读取、查询、导出和前端 GraphView 已跑通，图谱节点已进入章节上下文；Python 已产出中心度、桥接、关系分析、关键路径和增量构建摘要；人物、设定、剧情、时间线、伏笔、章节大纲、章节正文和样本技巧基础关系已入图；新增 `graph_artifacts` DB 业务表，保存图谱 JSONB、节点/边列表、节点/边类型分布、度统计、人物/地点/组织/物品计数、Top 节点、Top 中心度/桥接节点、关系分析、关键路径、增量摘要和图谱任务，支持 `GET /graph/db`、`POST /graph/sync`（项目级与 book 级）和 `graph_build` 成功后自动同步，前端新增“同步图谱数据库”按钮并展示高级分析摘要；图谱版本审计已支持手动归档、列表/详情和重建前自动归档，前端已有版本抽屉；缺真实 LLM 关系抽取和更强交互式图谱 |
| Phase 8 集成优化 | E2E、性能、错误处理、监控、文档 | 65% | Docker 构建、服务健康检查、API 烟测、前端浏览器路由烟测、模型配置 DB/加密烟测、Skill Profile DB 同步烟测、任务日志/文件日志 Tail/SSE 实时推送/可配置重试策略/resume 基础能力、workflow/checkpoint/人工确认烟测、导入任务阶段化进度、TaskCenter 全局任务监控、Dashboard 真实统计/服务健康/最近任务/性能窗口/基础告警中心/告警确认静默/基础趋势快照/趋势图表/采样保留策略/通知 outbox/基础升级判定/webhook-email 投递/回执重试/订阅人/分组路由/模板治理/投递历史筛选/只读运维看板、检索上下文烟测、统一产物管理烟测、内嵌返修烟测、独立 RevisionAgent 烟测、版本恢复烟测和人工审查烟测可用；GitHub Actions CI 已覆盖 Python/Java/前端主验证链路、固定 MVP smoke、Flyway 迁移校验和失败制品留存；仍缺覆盖率门槛、更多固定 E2E、性能回归样例、生产级连接治理和更完整权限审计 |

## 9. 当前最值得优先修的点

### P0：让可演示 MVP 稳定

1. 增加任务轮询和产物查看：
   - 样本页已能轮询运行中任务、显示 chunk/analysis 数量、报告路径和导入任务阶段化百分比；产物页已能统一查看、搜索、预览和下载项目文件
   - 已补请求头身份上下文与项目访问校验骨架；仍需 JWT/OAuth、用户/组织权限系统、生产级连接治理和更完整权限审计
2. 增加自动/手动单书汇总入口：
   - 样本页已提供手动单书汇总入口
3. 增加 Skill 生成和查看入口：
   - 后端已可生成、读取、编辑、启停、检测基础冲突、生成冲突审计报告、恢复内容版本、规则型质量校验、语义质量详情和批准/驳回审批，前端 ProjectDetail 已能展示列表、内容、来源/证据、编辑弹窗、启停按钮、质量校验、审批按钮、历史版本、配置版本恢复、冲突提示、报告预览下载、任务状态、重新生成和差异对比；下一步补真实模型语义评审和冲突自动合并
4. 建立固定 smoke 脚本：
   - 已有 `novel-system/scripts/smoke_mvp_pipeline.py` 覆盖离线 mock 链路：创建项目工作区 -> 样本导入 -> 全文分析 -> 覆盖率校验 -> 单书报告 -> 跨书归纳 -> Skill 生成。
   - 已固定验证每个样本的 `CHUNKED/ANALYZED` 语义状态，以及 `analysis/per_chunk`、`analysis/per_book`、`analysis/cross_book`、`skills/enabled.yaml` 等关键产物；对应 `tests/test_smoke_mvp_pipeline.py` 已通过。

### P1：把样本分析链路做完整

1. 统一分析输出路径到文档规范：
   - `analysis/per_chunk`
   - `analysis/per_book`
   - `analysis/cross_book`
   - `analysis/coverage`
2. 增加分析覆盖率：
   - 已新增 `coverage_check` Agent、Java 触发/读取 API 和前端覆盖率页签，可验证每个 chunk 是否成功分析。
   - 已新增失败/缺失 chunk 修复队列、`auto_repair` 自动补偿入口、前端“校验并自动修复”和定向修复入口。
   - 已新增 `/issues` 聚合诊断接口和前端问题块明细表；任务层已支持可配置 `retry_policy`/`max_retries` 和指数退避诊断；下一步补真实 LLM 下的失败原因分类和分析质量评估。
3. 增加失败 chunk 重试与断点续跑：
   - workflow 级 checkpoint/resume 已打通；`full_text_analysis` 已能逐 chunk 写 checkpoint、复用已落盘分析结果，并在 `/resume` 后跳过已完成 chunk。
   - `analysis_repair` 已能按覆盖率队列或指定 chunk 定向补偿缺失/失败分析；前端已能从问题块直接打开详情或发起单块修复。
4. 建立样本 chapters/chunks 的权威来源：
   - `sample_chapters`、`sample_chunks` 已落地，并提供结构同步/读取 API。
   - `analysis_results` 已落地，成功分析任务会自动同步 DB，手工 sync 接口也可重建 DB 状态。
   - 覆盖率任务已能回写 DB 缺失/失败诊断状态，失败分析文件会同步为 `FAILED`，对应 chunk 不再计入 processed。
5. 给前端提供产物列表 API：
   - 样本 manifest
   - 分块列表
   - 分析结果
   - 单书报告
   - 跨书报告
   - 以上已新增只读 API 并在样本页接入；统一 Artifact API 已提供分类、搜索、预览、下载、归档、恢复、归档永久删除、批量导出、差异对比、审计日志、敏感样本授权保护、操作级角色校验、请求头身份上下文和归档保留策略；后续补正式用户/组织权限系统。

### P2：补齐 Skill 与大纲核心约束

1. Skill 编辑、启停、优先级/scope 调整、基础冲突检测、冲突审计报告、版本列表、回滚、规则型质量校验、语义质量详情和批准/驳回审批已完成；`skill_profiles` DB 聚合快照和手动/自动同步入口已完成，并保存最新冲突报告摘要。
2. 为 Skill 继续增加真实模型语义评审和更深层冲突自动合并。
3. 前端已展示 `skills/local` 与 `enabled.yaml` 的主要信息并支持查看/编辑/对比/恢复内容版本、恢复配置版本、质量校验、质量详情、审批、重新生成和最近生成任务状态。
4. Project Soul 编辑、锁定/解锁、批准、版本列表、版本预览/恢复和前端查看已完成；大纲 JSON 版本列表、预览/恢复也已完成。
5. 章节大纲边界字段已进入 schema，`OutlineGeneratorAgent` 保存前会兜底补齐；`OutlineReviewAgent` 已支持规则审查、自动补齐和覆盖重建：
   - `core_goal`
   - `must_write`
   - `allowed_progress`
   - `must_not_write`
   - `reserved_for_future`
   - `stop_point`
   - `ending_hook`
6. 大纲已新增 `outline_artifacts` DB 业务表和同步/查询 API，且大纲生成、审查、手工保存后会自动同步；当前烟测已验证 `3/3` 章节边界统计、Project Soul 和最新审查报告可从 DB 读回。
7. 大纲批准前已增加边界字段完整性门槛，缺失字段默认拒绝批准，显式 override 会记录缺失项与人工强制放行；下一步支持逐章确认、差异对比和结构化编辑。

### P3：正文、记忆、图谱进入可用状态

1. 扩展 ContextBuilder：
   - 当前已汇入 Project Soul/章纲相关输入、前文摘要、Canon/记忆、Skill、图谱节点、关键词检索结果和本地 hybrid 检索结果。
   - 已写入 retrieval plan、hash-vector 摘要、RRF 融合结果、规则 rerank 分数、质量评估、引用预算和缓存状态。
   - 下一步补更细的来源去重、失败降级和生产级向量检索。
2. 把本地 hash-vector/rerank 雏形升级为生产级检索服务：
   - Java 的 vector/hybrid 摘要、检索配置、索引重建、质量评估、benchmark、缓存失效 API、质量报告 DB 同步和 benchmark 报告 DB 同步已完成。
   - 前端 RetrievalView 已能展示配置、索引摘要、上下文包、质量报告、benchmark、重建任务和索引版本。
   - ArtifactView 已能预览/下载索引摘要和检索报告。
   - 下一步接入 pgvector/LanceDB、真实 embedding 模型和外部 reranker。
3. 在已实现 BoundaryChecker、后续章纲保护、内嵌自动返修、独立 RevisionAgent、历史版本恢复、章节人工审查、章节差异对比和 `chapter_pipeline` 工作流人工确认的基础上，补真实 LLM 下的返修质量验证。
4. 在已区分 draft/final 且 final 自动触发记忆摄取的基础上，终稿多版本、终稿审计记录、草稿/终稿对比和批量发布已补齐；下一步补工作流级终稿确认。
5. 在现有项目级 Markdown/JSON 记忆、连续性报告和确定性修复基础上，继续补更多连续性规则、深度冲突解决和人工确认。
6. 在现有图谱读取/查询/导出和上下文包接入基础上补 NetworkX 中心度/路径/子图分析、增量更新和更丰富实体关系抽取。

## 10. 建议的近期里程碑

建议不要同时补所有 Phase，而是先完成一个“稳定可演示链路”：

1. 项目创建。
2. 样本上传。
3. `sample_import` 自动完成并回写 `CHUNKED`。
4. 前端正确展示样本标题、文件名、状态、章节数、分块数。
5. `full_text_analysis` 在 mock LLM 模式下完成所有 chunks。
6. `book_summary` 读取分析结果并生成单书报告。
7. `cross_book_synthesis` 支持至少 2 个样本汇总。已在 mock 链路验证。
8. `skill_generation` 生成项目 Skill 并写入 `skills/local`。已在 mock 链路验证。
9. 前端能查看任务状态、错误详情、报告和 Skill 产物路径。当前 ProjectDetail 已能展示任务和 Skill，SampleManagement 已能查看任务详情、单书/跨书报告，TaskCenter 已能全局查看任务、事件日志、失败诊断、checkpoint 恢复、人工确认审批和 retry policy；Skill 质量/审批/冲突报告已支持在 ProjectDetail 内预览和下载。
10. 生成 `novel/soul/project_soul.md`，作为大纲和正文的强制输入。已在 mock 链路验证。
11. 补齐大纲/章节读取 API，让前端能查看 Project Soul、outline 和 chapter draft。已验证。
12. 改造前端 ProjectDetail/OutlineEditor/ChapterWriter，让它们使用真实 books/outline/soul/chapter APIs，而不是占位和硬编码流程。已完成并通过浏览器路由烟测。
13. 补齐记忆读取 API 和 MemoryView，让前端能查看 6 类记忆、JSON 条目、快照和记忆任务。已完成并通过 API/浏览器烟测。
14. 补齐图谱构建、读取、查询、导出 API 和 GraphView，让前端能查看真实节点、关系和基础网络视图。已完成并通过 API/浏览器烟测。
15. 补齐最小 ContextBuilder 与 Retrieval API，让章节生成能引用样本、报告、Skill、记忆、图谱和前文摘要。已完成并通过第 2 章生成烟测。
16. 补齐章节终稿发布和终稿后自动记忆摄取。已完成并通过第 2 章终稿发布烟测。
17. 补齐章节边界检查和后续章纲保护清单。已完成并通过第 1/3 章边界烟测。
18. 补齐章节自动返修尝试和返修历史读取。已完成内嵌版并通过第 3 章边界阻塞返修烟测。
19. 补齐独立 RevisionAgent、章节返修 API、版本快照、`novel/reviews` 返修归档和前端返修入口。已完成并通过 `task_2d59c335759049a7` 烟测。
20. 补齐章节历史版本恢复 API 与前端恢复入口。已完成并通过 `POST /versions/chapter_3_v2_20260624191324/restore` 草稿恢复烟测，以及 `POST /versions/chapter_2_v1_20260624211017021/restore` 终稿恢复烟测。
21. 补齐章节人工审查 API、文档兼容简写入口、前端审查弹窗和 `chapter_human_review` 归档。已完成并通过 `POST /chapters/3/review` 烟测；`chapter_pipeline` 工作流人工确认和 TaskCenter 审批恢复入口也已可用；下一步补真实 LLM 下返修质量验证。
22. 补齐章节手工编辑保存 API、文档兼容简写入口、前端正文编辑 UI、编辑前快照和 `chapter_manual_edit` 归档。已完成并通过 `PATCH /chapters/3` 烟测。
23. 补齐大纲/Project Soul 编辑保存 API、文档兼容简写入口、前端编辑弹窗、编辑前快照和 `outline_manual_edit` 归档。已完成并通过 `PATCH /outline` 烟测。
24. 补齐 Skill 编辑保存、启停、优先级/scope 配置、基础冲突检测、前端管理入口、编辑前快照和 `skill_manual_edit` / `skill_config_edit` 归档。已完成并通过 `PATCH /skills/writing_skill` 与 `PATCH /skills/writing_skill/config` 烟测。
25. 补齐 Skill 内容版本列表、版本详情、版本恢复、恢复前快照、`skill_restore` 归档和前端历史版本恢复入口。已完成并通过 `POST /skills/writing_skill/versions/writing_skill_20260624203012691/restore` 烟测。
26. 补齐 OutlineReviewAgent、大纲审查 API、审查报告归档和前端审查报告入口。已完成并通过 `task_b0badab7e17e4dce` 烟测。
27. 补齐大纲边界字段自动补齐、覆盖重建、补齐前快照、`outline_boundary_fix` 归档和前端“补齐边界”入口。已完成并通过 `task_84ac8f36e7d4463c` 与 `task_854ecb76c48b4322` 烟测；大纲批准前边界完整性门槛和 override 审计已由 `BookArtifactServiceTest` 覆盖，下一步补逐章审批 UI 和结构化编辑。
28. 补齐终稿覆盖前快照、终稿版本递增、`chapter_finalize` 审计报告、恢复为终稿、前端“恢复为草稿/终稿”拆分操作、章节差异对比和批量终稿发布。已完成并通过第 2 章终稿覆盖发布与终稿恢复烟测，新增 `BookArtifactServiceTest` 草稿/终稿、历史版本差异对比和批量发布覆盖。
29. 补齐 Skill 规则型质量校验、批准/驳回审批、`skill_quality_check` / `skill_approval` / `skill_rejection` 报告归档、`skill_profiles` DB 聚合快照和前端操作入口。已完成并通过 `writing_skill` 校验/批准、`review_skill` 驳回后批准恢复、`POST /skills/profile/sync` 和 DB JSONB 查询烟测。
30. 补齐模型配置 profile 管理、DB 持久化、默认配置、结构/连通性测试、API Key 加密入库、API Key 掩码返回、任务默认 `model_profile_id` 注入和前端模型配置页。已完成并通过 `codex_smoke_mock` 默认配置、`codex_db_key_smoke` 加密存储/test、DB 密文查询、运行时 `apiKeyRef` 导出、Python secrets 引用解析、fallback/cache/cost/usage/embedding/rerank 和实际限流测试验证；下一步补真实供应商 API Key 调用、生产级配额/成本看板和集中 secrets provider/轮换治理。
31. 补齐统一产物管理 API 和前端产物页。已完成并通过 `/artifacts` overview、`/artifacts/list`、`/artifacts/view`、`/artifacts/download`、路径穿越 400 和前端 `/projects/20260624_164140_41ac/artifacts` 烟测；归档/恢复/归档永久删除、批量导出、差异对比、审计日志、敏感样本授权保护、角色门槛、拒绝审计、请求头身份上下文和归档保留策略已由 `ArtifactServiceTest` / `ArtifactControllerAccessTest` 覆盖；下一步补正式用户/组织权限系统。
32. 补齐记忆连续性报告的确定性修复闭环。已完成 `append_unique` 人物出场记录修复、`set_field first_mentioned` 人物/设定首次提及修复、`sort_by_chapter` 时间线排序修复、连续性/审计 fix API、修复前记忆版本、`memory_continuity_fix` / `memory_audit_fix` 事件归档和前端应用修复入口，并通过 Python/Java/前端自动化验证；下一步扩展更多连续性问题类型和人工确认流。
33. 补齐检索质量报告 DB 同步。已完成 `retrieval_artifacts.quality_report_path` / `quality_report` 字段、Flyway V3 迁移、`RetrievalArtifactDbService` 质量报告读取与 metadata 汇总，并通过 `RetrievalArtifactDbServiceTest` 和既有缓存失效测试验证；下一步补正式向量库、真实 embedding 和外部 reranker。
34. 补齐检索索引版本治理。已完成 `indexes/versions/retrieval_index_{timestamp}.json` 版本快照、`GET/POST /retrieval/versions` 和 `GET /retrieval/versions/{versionId}`、重建前自动归档、缓存失效版本兼容列表和前端 RetrievalView 版本抽屉，并通过 `RetrievalArtifactServiceTest` 与前端 build 验证；下一步补生产级向量库、真实 embedding/rerank 和外部 reranker。
35. 补齐离线检索效果基准集。已完成 `indexes/retrieval_benchmark.json` 查询集读取、`indexes/retrieval_benchmark_report.json` 报告、hit rate/MRR/recall@k/平均质量分/逐用例命中明细、Java `/retrieval/benchmark` 读取与 `/retrieval/benchmark/evaluate` 触发、前端 RetrievalView benchmark 展示，并通过 Python/Java 定向测试验证；下一步补真实向量库与外部 reranker 后的生产级基准。
36. 补齐检索 benchmark 报告 DB 同步。已完成 `retrieval_artifacts.benchmark_report_path` / `benchmark_report` 字段、Flyway V4 迁移、`RetrievalArtifactDbService` benchmark 报告读取、metadata 汇总和列表摘要指标返回，并通过 `RetrievalArtifactDbServiceTest` 验证；下一步补真实向量库与外部 reranker 后的生产级 benchmark 回归。
37. 补齐 Skill 冲突检测报告化和 DB 元数据同步。已完成 `POST /skills/conflicts/report`、`skill_conflict_report` JSON 审计归档、冲突 severity 汇总、处理建议、`skill_profiles.skill_metadata.latestConflictReport` 同步和前端 ProjectDetail “生成冲突报告”入口，并通过 `SkillServiceSemanticQualityTest`、Java 全量测试和前端 build 验证。
38. 补齐 Skill 历史版本对比当前。已完成 `GET /skills/{skillName}/versions/{versionId}/diff`，可返回版本快照与当前 Skill 的左右摘要、截断标记、added/removed/changed 行数和行级 diff；ProjectDetail 历史版本弹窗新增“对比当前”和差异弹窗，并通过 `SkillServiceSemanticQualityTest` 与前端 build 验证。
39. 补齐 Skill 重新生成入口和生成状态反馈。已完成 ProjectDetail “重新生成Skills”按钮、`skill_generation` 参数透传、跨书归纳依赖提示、最近生成任务状态展示和暂无 Skill 时的生成入口，并通过前端 build 验证。
40. 补齐 Skill 生成结果预览。已完成 ProjectDetail “查看生成结果” Drawer，展示最近 `skill_generation` 任务的生成数量、样本数量、`enabled_yaml`、`generated_skills` 表格、`outputRefs` 和原始 result JSON，并通过前端 build 验证。
41. 补齐 Skill 报告预览/下载入口。已完成 ProjectDetail Skill 报告 Drawer，质量详情、Skill 列表、冲突提示和批准/驳回动作可直接预览或下载质量/审批/驳回/冲突 JSON 报告，并通过前端 build 验证。
42. 补齐大纲 JSON 历史版本治理。已完成大纲版本列表、版本详情、锁定保护恢复、恢复前快照、恢复后治理状态回到 `pending_review`、`outline_artifacts` 同步和 OutlineEditor “大纲版本”抽屉，并补 `BookArtifactServiceTest` 定向覆盖。
43. 补齐记忆时间线排序自动修复验证。已完成 `sort_by_chapter` 审计 fix 的 Java 定向测试，覆盖 `timeline.json` 重排、修复前版本、报告状态更新和 `memory_audit_fix` 事件归档。
44. 补齐伏笔节点基础关系入图。已完成 `GraphBuilderAgent` 对 `suspenses.json` 的 `involved_characters` / `involved_settings` / `resolved_by_plot` 抽取，生成 `involves_character`、`involves_setting` 和 `resolved_by` 边，并扩展 Python `test_graph_analysis.py` 验证伏笔-人物与伏笔-剧情关系。
45. 补齐大纲章节结构入图。已完成 `GraphBuilderAgent` 读取 `novel/outline/{bookId}_outline.json` 并生成 `chapter_outline` 节点，按已知人物/设定/伏笔/剧情名称从章节目标、场景、伏笔和边界字段中确定性匹配关系，生成章节到人物、地点、伏笔和剧情的图谱边，并扩展 Python `test_graph_analysis.py` 验证。
46. 补齐章节正文确定性入图。已完成 `GraphBuilderAgent` 读取终稿/草稿章节 JSON，生成 `chapter_content` 节点，连接对应 `chapter_outline`，并按正文内容匹配已知人物、设定、伏笔和剧情节点生成 mentions 关系；已扩展 Python `test_graph_analysis.py` 验证正文-大纲、正文-人物、正文-地点、正文-伏笔和正文-剧情关系。
47. 补齐样本技巧关系入图。已完成 `GraphBuilderAgent` 读取 `analysis/cross_book/technique_summary.json` / 根目录兼容技巧汇总，生成 `technique` 节点，并按大纲/正文内容匹配技巧名生成 `uses_technique` 边；已扩展 Python `test_graph_analysis.py` 验证大纲技巧和正文技巧关系。

这个闭环跑稳后，再推进 Project Soul、大纲、正文、记忆和图谱，会比现在直接铺所有功能更可控。

## 11. 总结

当前项目最有价值的进展是：**底层服务已经能容器化构建和启动，Java/Python 协议已基本拉齐，模型配置 DB 化与 API Key 加密、Skill Profile DB 同步、样本导入、样本分析、Skill 生成与管理、大纲、Project Soul、大纲审查、边界自动补齐、章节草稿/终稿、章节边界检查、内嵌自动返修、独立 RevisionAgent、章节版本恢复、章节人工审查、章节手工编辑保存、终稿记忆摄取、记忆连续性报告自动修复、图谱查看、章节检索上下文、统一产物浏览、最小工作流 checkpoint/resume 和全文分析逐块断点续跑这些 MVP 链路已经真实跑通。**

当前最大的缺口是：**“分析沉淀 -> Skill -> 大纲 -> 正文 -> 记忆/图谱/检索”的业务链路虽已有局部闭环，导入/分析等关键任务也开始具备 checkpoint 化进度，但仍缺更完整跨阶段状态机、真实模型调用与降级、生产级向量检索/外部 rerank、高级图谱分析、更多真实 LLM 质量验证、完整用户/组织/RBAC 权限系统和更强 CI/CD 质量门禁。**

下一步最推荐先把“章节生成 -> 上下文包 -> 章节审查 -> 返修 -> 终稿版本 -> 记忆一致性检查/修复 -> 人工确认”的小闭环打牢，而不是立刻铺完整长篇创作系统。这个闭环稳定后，Project Soul、大纲、正文、记忆和图谱才会形成真正可持续的生产线。
