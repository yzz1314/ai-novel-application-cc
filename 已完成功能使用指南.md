# AI 小说创作系统已完成功能使用指南

> 适用分支：`codex/memory-continuity-check`
> 更新时间：2026-06-28
> 目标读者：本地试用、验收和继续开发本项目的人

本文只描述当前代码中已经接通的功能入口和推荐使用流程。未完成项包括：真实供应商模型大规模回归、生产级向量检索压测、迁移回滚/线上变更演练、正式 OAuth/OIDC 登录、完整资源级 RBAC、更多真实 LLM 质量评估。

## 1. 启动系统

推荐使用根目录 Docker Compose，一次启动 PostgreSQL + pgvector、Redis、Java 后端、Python AI 服务和前端。

```powershell
cd D:\code\ai-novel-application-cc
docker compose up -d --build
```

如果只想离线跑通流程，可以启用 mock LLM：

```powershell
$env:MOCK_LLM="true"
docker compose up -d --build
```

服务启动后访问：

| 服务 | 地址 |
| --- | --- |
| 前端 | http://localhost/ |
| Java 后端健康检查 | http://localhost:8080/api/health |
| Java Swagger | http://localhost:8080/swagger-ui.html |
| Python 健康检查 | http://localhost:8000/health |
| Python API 文档 | http://localhost:8000/docs |
| PostgreSQL + pgvector | localhost:5432，库名 `novel_system` |

常用运维命令：

```powershell
docker compose ps
docker compose logs -f java-backend
docker compose logs -f python-ai
docker compose restart java-backend python-ai frontend
docker compose down
```

## 2. 推荐主流程

完整创作链路建议按这个顺序使用：

1. 创建项目
2. 上传样本 TXT
3. 样本导入、全文分析、覆盖率检查、单书报告
4. 多样本跨书归纳
5. 生成并治理项目 Skills
6. 生成 Project Soul 和大纲
7. 审查/补齐/批准大纲
8. 生成章节草稿
9. 章节返修、人工审查、发布终稿
10. 终稿触发记忆摄取
11. 构建图谱和检索索引
12. 通过任务中心、产物中心、Dashboard 做治理和排障

## 3. 页面入口

| 页面 | 地址 | 用途 |
| --- | --- | --- |
| 工作台 | `/dashboard` | 查看项目、任务、健康状态、告警、趋势、运维看板 |
| 项目管理 | `/projects` | 创建项目、搜索筛选、批量归档、进入项目详情 |
| 项目详情 | `/projects/{projectId}` | 查看项目进度、样本、Skills、工作流入口 |
| 样本管理 | `/projects/{projectId}/samples` | 上传样本、查看分块/分析/覆盖率/问题块/报告 |
| 大纲编辑 | `/projects/{projectId}/outline` | 生成、编辑、审查、补齐、批准和恢复大纲 |
| 章节创作 | `/projects/{projectId}/chapters` | 生成章节、返修、审查、编辑、发布终稿 |
| 记忆管理 | `/projects/{projectId}/memory` | 查看记忆、快照、连续性检查和自动修复 |
| 知识图谱 | `/projects/{projectId}/graph` | 构建、查询、同步、导出和版本化图谱 |
| 检索管理 | `/projects/{projectId}/retrieval` | 配置/重建/评估/版本化检索索引 |
| 产物管理 | `/projects/{projectId}/artifacts` | 预览、下载、归档、恢复、差异对比、审计 |
| 任务中心 | `/tasks` | 查看、重试、恢复、取消任务，查看日志和审批 |
| 模型配置 | `/models` | 管理模型 profile、API Key、默认模型和连通性测试 |
| 访问治理 | `/access` | 管理组织/项目成员、角色策略和访问审计 |

## 4. 模型配置

本地演示建议先用 mock 模式。真实模型调用可以在前端模型配置页完成。

1. 打开 `/models`。
2. 创建模型 Profile。
3. 配置主模型、快速模型、Embedding 模型、Rerank 模型。
4. 填写 API Key。后端会加密入库，接口只返回掩码。
5. 点击测试，确认连通性。
6. 设为默认配置。

说明：

- Python 运行时会读取 Java 导出的模型配置。
- API Key 会通过 `apiKeyRef` 和独立 secrets 文件隔离，运行时配置不直接落明文 key。
- 已支持 fallback、缓存、限流、token/成本估算、usage 台账、embedding/rerank 本地降级。
- 真实供应商 API Key 的完整回归仍属于后续工作。

## 5. 创建项目

1. 打开 `/projects`。
2. 点击创建项目。
3. 填写项目名称、题材、描述等信息。
4. 创建后进入 `/projects/{projectId}`。

项目详情页会展示当前进度、最近任务、样本、Skills、Books，以及内置工作流入口。

## 6. 上传和分析样本

样本推荐使用 TXT 文件，最好是章节边界清晰的长文本。

1. 进入 `/projects/{projectId}/samples`。
2. 上传样本 TXT。
3. 上传后会创建 `sample_import` 任务，完成文本规范化、章节检测、分块和 manifest 生成。
4. 在样本详情中查看：
   - Manifest
   - 分块列表
   - 分析结果
   - 覆盖率
   - 问题块
   - 单书报告

可用操作：

| 操作 | 作用 |
| --- | --- |
| 全文分析 | 执行 `full_text_analysis`，逐 chunk 分析样本 |
| 覆盖率检查 | 执行 `coverage_check`，检查文本分块和分析覆盖情况 |
| 自动修复 | 对缺失/失败 chunk 执行定向 `analysis_repair` |
| 单书报告 | 执行 `book_summary`，生成单样本拆解报告 |
| 跨书归纳 | 对多个样本执行 `cross_book_synthesis` |
| 导出分析包 | 导出样本信息、覆盖率、问题块、报告和分块索引 |

项目详情页也提供工作流：

| 工作流 | 作用 |
| --- | --- |
| `sample_analysis` | 单样本：全文分析 -> 覆盖率检查 -> 单书总结 |
| `parallel_sample_analysis` | 双样本并行分析后自动跨书归纳 |

## 7. Skills 使用和治理

跨书归纳完成后，可以生成项目专属 Skills。

入口：`/projects/{projectId}` 的 Skills 区域。

可用功能：

- 生成/重新生成 writing、outline、review 三类 Skill。
- 查看 Skill 内容、来源、证据和启用状态。
- 编辑 Skill Markdown。
- 启用/停用 Skill。
- 调整优先级、type、scope。
- 查看内容版本、对比当前版本、恢复历史版本。
- 查看配置版本、恢复配置版本。
- 运行规则型质量校验。
- 查看语义质量详情。
- 批准或驳回 Skill。
- 生成冲突检测报告。
- 预览/下载质量、审批、驳回、冲突报告。

建议用法：

1. 至少完成两本样本分析和跨书归纳。
2. 点击重新生成 Skills。
3. 对每个 Skill 运行质量校验。
4. 检查冲突报告。
5. 通过后批准 Skill。
6. 后续大纲和章节生成会读取项目级 Skill。

## 8. 大纲和 Project Soul

入口：`/projects/{projectId}/outline`。

可用功能：

- 生成 Project Soul。
- 生成总纲/卷纲/章纲。
- 编辑并保存 Project Soul。
- 编辑并保存大纲 JSON。
- 锁定/解锁 Project Soul 和大纲。
- 大纲审查。
- 自动补齐章节边界字段。
- 大纲批准。
- 大纲版本列表、详情预览和恢复。

大纲批准前会检查每个章纲的边界字段：

- `core_goal`
- `must_write`
- `allowed_progress`
- `must_not_write`
- `reserved_for_future`
- `stop_point`
- `ending_hook`

缺失字段时默认拒绝批准；如确实要强制放行，可使用 override，系统会记录缺失项和人工放行痕迹。

## 9. 章节创作

入口：`/projects/{projectId}/chapters`。

可用功能：

- 按 book、卷号、章节号生成章节草稿。
- 自动加载 Project Soul、章纲、Skills、记忆、图谱和检索上下文。
- BoundaryChecker 检查越界、剧透、后续章纲污染等问题。
- 内嵌自动返修。
- 独立 RevisionAgent 返修。
- 手工编辑章节正文并保存。
- 查看章节审查报告。
- 人工批准/驳回章节。
- 查看章节历史版本。
- 对比草稿/终稿/历史版本。
- 恢复为草稿或终稿。
- 发布单章终稿。
- 批量发布终稿。

推荐流程：

1. 先确保大纲已通过审查或至少边界字段完整。
2. 进入章节页，选择 book、卷号、章节号。
3. 生成章节草稿。
4. 查看边界检查和审查结果。
5. 有问题时执行返修或手工编辑。
6. 人工审查通过后发布终稿。
7. 终稿发布后会触发记忆摄取相关链路。

## 10. 记忆系统

入口：`/projects/{projectId}/memory`。

已支持 6 类记忆：

- 人物状态
- 伏笔
- 时间线
- 人物关系
- 角色认知
- 世界/设定

可用功能：

- 查看 Markdown 记忆。
- 查看 JSON 记忆条目。
- 查看章节快照。
- 手动同步记忆数据库。
- 创建记忆版本。
- 查看/恢复记忆版本。
- 运行连续性检查。
- 查看连续性报告。
- 对报告问题填写处理结论。
- 对可自动修复的问题直接应用修复。
- 运行记忆审计。

当前已支持的自动修复类型：

| 问题 | 修复 |
| --- | --- |
| 人物出场记录未更新 | `append_unique` 追加记录 |
| 人物提前出场 | `set_field first_mentioned` 修正首次提及章节 |
| 设定提前揭示 | `set_field first_mentioned` 修正首次提及章节 |
| 时间线排序倒挂 | `sort_by_chapter` 按章节重排时间线 |

应用修复前会创建记忆版本，修复后会写入事件归档。

## 11. 知识图谱

入口：`/projects/{projectId}/graph`。

可用功能：

- 构建项目级或 book 级图谱。
- 读取图谱节点/边。
- 查询图谱路径。
- 导出图谱 JSON。
- 同步图谱数据库。
- 查看高级分析摘要。
- 创建图谱版本。
- 查看图谱版本列表和详情。
- 重建前自动归档当前图谱版本。

图谱当前可纳入：

- 人物
- 设定
- 剧情
- 时间线
- 伏笔
- 章节大纲
- 章节正文
- 样本技巧

当前关系主要来自确定性规则和已有产物，真实 LLM 关系抽取仍是后续工作。

## 12. 检索和上下文系统

入口：`/projects/{projectId}/retrieval`。

检索系统用于给章节创作提供上下文包。当前可汇入：

- 样本 chunk
- 单书/跨书报告
- Skills
- Project Soul
- 大纲
- 章节正文
- 记忆
- 图谱

可用配置：

| 配置 | 说明 |
| --- | --- |
| 关键词检索 | BM25 风格本地检索 |
| 向量检索 | hash-vector 或模型 embedding |
| 图谱检索 | 使用图谱节点和关系补上下文 |
| Rerank | rules、auto 或 LLM Gateway rerank |
| Vector backend | auto、memory、LanceDB、pgvector |
| pgvector ANN | auto、HNSW、IVFFlat、none |
| HNSW 参数 | `m`、`ef_construction`、`ef_search` |
| IVFFlat 参数 | `lists`、`probes` |
| 上下文预算 | top_k、最大字符数、单结果长度、样本引用长度等 |

常用操作：

- 保存检索配置。
- 重建索引。
- 同步检索数据库。
- 运行质量评估。
- 运行离线 benchmark。
- 清理旧索引缓存。
- 创建索引版本。
- 查看版本详情。
- 查看章节 context pack。

产物位置通常在项目工作区的：

- `indexes/bm25`
- `indexes/vector`
- `indexes/hybrid`
- `indexes/retrieval_config.json`
- `indexes/retrieval_index_report.json`
- `indexes/retrieval_quality_report.json`
- `indexes/retrieval_benchmark_report.json`
- `indexes/versions`

## 13. pgvector 演练脚本

本轮新增了真实 PostgreSQL + pgvector 演练脚本，用于验证迁移、向量写入、HNSW/IVFFlat ANN 查询和报告输出。

在根目录 Docker Compose 已启动的情况下运行：

```powershell
cd D:\code\ai-novel-application-cc\novel-system
python scripts\pgvector_rehearsal.py `
  --dsn postgresql://novel_user:novel_pass@localhost:5432/novel_system `
  --workspace workspace `
  --project-id local_pgvector_rehearsal `
  --keep-workspace `
  --require-dsn
```

报告输出：

```text
novel-system/workspace/projects/local_pgvector_rehearsal/indexes/pgvector_rehearsal_report.json
```

报告中应看到：

- `status=passed`
- `migration.status=passed`
- `retrieval_vectors_exists=true`
- HNSW run `status=passed`
- IVFFlat run `status=passed`
- 固定查询 `hit_rate=1.0`

如果本机 Python 没装依赖，可在 Python 服务环境或临时 venv 中安装 requirements 后运行。CI 已在 Python job 中自动启动 `pgvector/pgvector:pg15` 并运行该演练。

## 14. 任务中心

入口：`/tasks`。

可用功能：

- 按项目和状态筛选任务。
- 查看任务详情。
- 查看结构化事件日志。
- 查看 JSONL 文件日志 tail。
- 通过 SSE 实时刷新任务日志。
- 查看进度、checkpoint、retry policy。
- 失败任务重试。
- 支持 resume 的任务恢复。
- 取消运行中任务。
- 处理人工确认任务。

常见任务类型：

| 任务 | 说明 |
| --- | --- |
| `sample_import` | 样本导入、规范化、章节检测、分块 |
| `full_text_analysis` | 逐 chunk 样本分析 |
| `coverage_check` | 覆盖率检查 |
| `analysis_repair` | 缺失/失败 chunk 修复 |
| `book_summary` | 单书报告 |
| `cross_book_synthesis` | 跨书归纳 |
| `skill_generation` | Skill 生成 |
| `outline_generation` | 大纲生成 |
| `outline_review` | 大纲审查 |
| `chapter_writing` | 章节生成 |
| `chapter_revision` | 章节返修 |
| `memory_extraction` | 记忆摄取 |
| `graph_build` | 图谱构建 |
| `retrieval_index` | 检索索引重建 |
| `workflow` | 编排工作流 |

## 15. 产物管理

入口：`/projects/{projectId}/artifacts`。

可用功能：

- 按分类查看项目产物。
- 搜索文件路径。
- 预览文本/JSON/Markdown。
- 下载单个产物。
- 批量 ZIP 导出。
- 归档产物。
- 恢复归档产物。
- 永久删除归档产物。
- 文本差异对比。
- 查看审计日志。
- 执行 30 天归档保留策略。
- 敏感样本产物授权保护。

注意：

- 归档不是物理删除，会移动到 `artifacts/archive` 并写审计日志。
- 永久删除只针对归档文件，删除后无法从产物页恢复。
- 敏感样本完整预览、下载、diff、批量导出需要具备相应角色。

## 16. Dashboard 和告警

入口：`/dashboard`。

可用功能：

- 项目/样本/章节/任务统计。
- 服务健康状态。
- 最近项目和最近任务。
- 工作流进度。
- 阻塞项目。
- 下一步动作。
- 任务性能窗口。
- 慢任务、高重试、失败任务提示。
- 基础告警中心。
- 告警确认、静默和重新打开。
- 长期趋势快照。
- 趋势折线图。
- 告警通知 outbox。
- webhook/email 投递状态。
- 通知策略配置。
- 投递历史筛选。
- 只读运维看板。

## 17. 访问治理

入口：`/access`。

当前已支持：

- 查看当前请求身份。
- 查看组织和组织成员。
- 添加/移除组织成员。
- 查看项目成员。
- 添加/移除项目成员。
- 查看访问审计。
- 查看和编辑角色策略。
- JWT Bearer 鉴权骨架。
- 请求头身份上下文。

说明：

- 当前不是完整 OAuth/OIDC 登录系统。
- 本地演示可通过请求头或前端 API 配置传递身份上下文。
- 更细的资源级 RBAC 仍是后续工作。

## 18. 常见问题

### 前端打不开

先确认容器状态：

```powershell
docker compose ps
docker compose logs -f frontend
```

当前 Docker 前端端口是 `80`，访问 `http://localhost/`。

### Java 后端启动失败

查看日志：

```powershell
docker compose logs -f java-backend
```

常见原因：

- PostgreSQL 未健康。
- Flyway 迁移失败。
- `SPRING_JPA_HIBERNATE_DDL_AUTO` 不是 `validate`。
- 数据库旧 volume 与当前 schema 不兼容。

### Python AI 服务启动失败

查看日志：

```powershell
docker compose logs -f python-ai
```

常见原因：

- `.env` 模型配置错误。
- 依赖镜像未重建。
- `PROJECT_BASE_PATH` 或 workspace volume 异常。

### 真实模型调用失败

先用 `/models` 的测试按钮检查 profile。再确认：

- API Key 是否填写。
- base URL 是否正确。
- provider/model 名称是否匹配。
- 是否启用 mock。
- 是否触发 fallback。

### pgvector 演练失败

检查 PostgreSQL：

```powershell
docker compose ps postgres
docker compose logs -f postgres
```

确认 DSN：

```text
postgresql://novel_user:novel_pass@localhost:5432/novel_system
```

如果报 Python 缺依赖，请在安装了 `psycopg[binary]`、`pydantic`、`pydantic-settings` 的环境中运行，或直接依赖 CI 执行。

## 19. 当前最推荐的验收路径

如果你想快速验证核心已完成功能，按下面顺序做：

1. `docker compose up -d --build`
2. 打开 `http://localhost/`
3. 创建项目
4. 上传 2 个 TXT 样本
5. 在项目详情运行 `parallel_sample_analysis`
6. 生成 Skills，运行质量校验和冲突报告
7. 生成 Project Soul 和大纲
8. 运行大纲审查，补齐边界字段，批准大纲
9. 生成第 1 章草稿
10. 查看检索上下文和边界检查
11. 返修或手工编辑后发布终稿
12. 在记忆页运行连续性检查
13. 在图谱页重建图谱
14. 在检索页启用 pgvector，重建索引，运行 benchmark
15. 在产物页导出项目产物包
16. 在任务中心查看所有任务日志和失败重试能力

完成这条路径后，当前 MVP 的主要已完成能力基本都能被覆盖。
