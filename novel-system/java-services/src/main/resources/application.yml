spring:
  application:
    name: novel-system
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/novel_system}
    username: ${SPRING_DATASOURCE_USERNAME:novel_user}
    password: ${SPRING_DATASOURCE_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:validate}
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  flyway:
    enabled: ${SPRING_FLYWAY_ENABLED:true}
    locations: classpath:db/migration
    baseline-on-migrate: true
    schemas: ${SPRING_FLYWAY_SCHEMAS:public}

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  web:
    resources:
      add-mappings: true
  mail:
    host: ${SPRING_MAIL_HOST:}
    port: ${SPRING_MAIL_PORT:25}
    username: ${SPRING_MAIL_USERNAME:}
    password: ${SPRING_MAIL_PASSWORD:}

server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# Python服务配置
python:
  service:
    url: ${PYTHON_SERVICE_URL:http://localhost:8000}

# 访问控制配置。默认兼容本地开发；生产可通过环境变量开启强制请求头身份与项目访问校验。
security:
  access:
    enforce: ${ACCESS_CONTROL_ENFORCE:false}
    require-authentication: ${ACCESS_CONTROL_REQUIRE_AUTH:false}
    jwt:
      enabled: ${ACCESS_JWT_ENABLED:false}
      require-bearer: ${ACCESS_JWT_REQUIRE_BEARER:false}
      secret: ${ACCESS_JWT_SECRET:}
      issuer: ${ACCESS_JWT_ISSUER:}
      audience: ${ACCESS_JWT_AUDIENCE:}
      clock-skew-seconds: ${ACCESS_JWT_CLOCK_SKEW_SECONDS:60}

# 文件存储配置
dashboard:
  alerts:
    delivery-enabled: ${DASHBOARD_ALERT_DELIVERY_ENABLED:true}
    delivery-max-attempts: ${DASHBOARD_ALERT_DELIVERY_MAX_ATTEMPTS:3}
    delivery-retry-delay-minutes: ${DASHBOARD_ALERT_DELIVERY_RETRY_DELAY_MINUTES:15}
    webhook-url: ${DASHBOARD_ALERT_WEBHOOK_URL:}
    email-to: ${DASHBOARD_ALERT_EMAIL_TO:}
    email-from: ${DASHBOARD_ALERT_EMAIL_FROM:}
  trends:
    sample-interval-minutes: ${DASHBOARD_TREND_SAMPLE_INTERVAL_MINUTES:5}
    retention-days: ${DASHBOARD_TREND_RETENTION_DAYS:30}

file:
  storage:
    base-path: ${PROJECT_BASE_PATH:/workspace}

# 日志配置
logging:
  level:
    root: INFO
    com.novel.system: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n"
